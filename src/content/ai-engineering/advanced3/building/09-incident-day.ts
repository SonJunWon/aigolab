import type { Lesson } from "../../../../types/lesson";
import type { Quiz } from "../../../../types/quiz";

/**
 * 고급3 시리즈C(운영 만들기) 09강 — 실전 ③ 사고의 날.
 * 운영 30일 아크 3주차: 드리프트 사고의 감지→분류→재현→임시조치→포스트모템→박제 전 과정.
 */
export const lessonC09: Lesson = {
  id: "ai-eng-a3-c09-incident-day",
  language: "ai-engineering",
  track: "advanced3",
  order: 29,
  title: "C9. 🏗 실전 ③ 사고의 날 — 자동 진단과 포스트모템",
  subtitle: "운영 30일 아크 3주차: 모델이 조용히 바뀐 날, 운영 계층이 일하는 법",
  estimatedMinutes: 55,
  cells: [
    {
      type: "markdown",
      source: `# 🏗 고급3·C9강. 실전 ③ 사고의 날
### — 운영 30일 아크 3주차

⏱️ 55분 · 🧰 API 키 · 🗺️ 운영 루프 전 국면 총동원 · 선수: C1~C8

> 💡 **3주차 시나리오**: 어느 아침, 오탐률이 3배로 뛰었습니다. 코드? 그대로.
> 지침? 그대로. — **모델 제공사가 조용히 업데이트한 것** (A1 드리프트의 두 번째 범인).
> 오늘은 지금까지 지은 부품 전부가 실전 투입되는 날입니다:
> 경보(C2) → 분류(C5) → 재현(C1의 시간 주입!) → 임시 조치(문턱) →
> 포스트모템 초안 자동 생성 → 골든 박제(C6). **사고 대응의 전 과정을 코드로 완주.**`,
    },
    {
      type: "llm-code",
      language: "typescript",
      source: `// ══ 사고의 날 — 대응 전 과정 시뮬레이션 ══
// (드리프트를 mock으로 주입: 3주차부터 '검증관'이 갑자기 관대해져 오탐이 통과된다)

type RunRecord = { id: string; ts: number; version: string; ok: boolean; falsePos: number; findings: number };
const DAY = 86400000, base = 1700000000000;
const LOG: RunRecord[] = [];
for (let d = 0; d < 18; d++) {
  const drifted = d >= 14;                    // 15일째: 모델 업데이트 발생 (아무도 모름)
  LOG.push({
    id: \`run-\${d}\`, ts: base + d * DAY, version: "v13", ok: true,
    findings: 3 + (d % 2),
    falsePos: drifted ? 2 + (d % 2) : (d % 4 === 0 ? 1 : 0),   // 오탐 급증!
  });
}

// ── 1. 경보 (C2 배관): 오탐률 추이가 문턱을 넘는다 ──
const week = (from: number, to: number) => LOG.slice(from, to);
const fpRate = (rs: RunRecord[]) => +(rs.reduce((a, r) => a + r.falsePos, 0) / rs.reduce((a, r) => a + r.findings, 0)).toFixed(2);
const baseline = fpRate(week(0, 14)), current = fpRate(week(14, 18));
console.log(\`📈 오탐률: 평소 \${baseline} → 최근 \${current}\`);
if (current > baseline * 2) console.log("🔴 [act] 오탐률 2배 초과 — 대응 개시\\n");

// ── 2. 분류 (C5): 어떤 실패인가 ──
console.log("🩺 분류: quality_drop (형식·도구·예산 모두 정상인데 품질만 하락) → 드리프트 의심");
console.log("   처방: 골든 재실행 + 버전·기간 대조\\n");

// ── 3. 재현·격리 (C1의 시간 주입이 배당금을 주는 순간) ──
// 같은 골든 PR을 '평소 기간'과 '최근 기간' 레코드로 대조 — 코드·지침이 같은데 결과가 다르면 범인은 모델
console.log("🔬 재현: 골든 PR 5건을 현재 시점에 재실행 → 오탐 3건 (평소 기록: 0건)");
console.log("   코드 diff: 없음 / 지침 diff: 없음 / 기간: 15일째부터 → 범인: 외부(모델) 변화 확정\\n");

// ── 4. 임시 조치: 문턱 상향 (고급2 C8의 다수결 다이얼!) ──
// 검증관이 관대해졌으니 다수결 문턱을 2/3 → 3/3으로 임시 상향 — 오탐을 다시 조인다
console.log("🚑 임시 조치: 다수결 문턱 2/3 → 3/3 (구조 변경 아님 — 설정 다이얼, 즉시 적용·즉시 복원 가능)");
console.log("   효과 확인: 골든 재실행 → 오탐 0건, 단 재현율 -10% (트레이드오프 기록)\\n");

// ── 5. 포스트모템 초안 자동 생성 — 레코드가 타임라인을 쓴다 (B6 양식) ──
const pm = \`# 포스트모템: 오탐률 급증 (v13, Day15~18)
## 요약
등급 2 (오탐 리뷰가 사용자에게 도달) · 영향: 4일간 리뷰 신뢰도 저하 · 지속: 감지까지 3일
## 타임라인 (run 레코드에서 자동 복원)
- Day15: 오탐률 상승 시작 (레코드 상 첫 이상)
- Day18: 텔레메트리 경보 → 대응 개시  ← **감지 지연 3일이 핵심 발견**
- Day18+1h: 분류(quality_drop) → 재현으로 외부 변화 확정 → 문턱 상향 임시 조치
## 원인 (구조)
외부: 모델 제공사 업데이트로 검증관 관대화 (드리프트 범인 ②)
내부: 오탐률이 계기판 3숫자에 없어 경보가 '2배' 문턱에서야 발화 — 관측의 구멍
## 구조적 방지책 (개선 대장 행)
1. 오탐률(정확히는 '깨끗한 골든의 발견 수')을 주간 자동 골든 실행으로 상시 감시 — 감지 지연 3일 → 1일
2. 모델 버전 변경 감지 시(응답 헤더/공지) 골든 자동 실행 트리거
## 박제
Day15 오탐 사례 3건 → 골든 세트 추가 (다음 회귀 게이트부터 시험 문제)\`;
console.log("📄 포스트모템 초안 (자동 생성):\\n" + pm);
console.log("\\n👉 사람이 한 일은 판단 2개뿐 — '문턱 상향' 결정과 포스트모템 확정. 나머지는 전부 지어둔 부품이 했다.");`,
      hints: [
        "타임라인이 기억이 아니라 run 레코드에서 복원되는 것 — B6 '기록은 사람을 지킨다'의 완성형이에요. 감지 지연 3일이라는 뼈아픈 숫자도 레코드만이 정직하게 말해줍니다.",
        "임시 조치로 '문턱 상향'(설정 다이얼)을 먼저 쓰고 구조 변경은 포스트모템으로 넘기는 순서 — 사고 중에는 되돌리기 쉬운 조치만(B6 멈춤 우선의 정신). 다이얼은 즉시 복원 가능하니까요.",
        "방지책 1의 '깨끗한 골든의 발견 수 상시 감시' — 고급2 C9의 오탐 함정 골든이 운영 계기판으로 승격된 것. 3부작의 부품들이 계속 서로를 찾아갑니다.",
      ],
    },
    {
      type: "markdown",
      source: `## 🧯 사고 대응의 코드적 순서 — B6 카드의 실행판

| B6 첫 행동 카드 | 오늘의 코드 대응 |
|----------------|-----------------|
| 멈춘다 | (이번 사고는 정지 대신) 문턱 상향 — 피해 억제의 다이얼 버전 |
| 보존한다 | run 레코드는 이미 불변 로그 — 지울 수도 없다 (설계의 승리) |
| 원인 | 분류기 → 재현 → 기간·diff 대조로 격리 |
| 방지책 | 개선 대장 행으로 — **방지책도 회귀 게이트(C6)를 통과해야 반영** |
| 박제 | 사고 사례 → 골든 세트 → 다음 게이트부터 자동 시험 |

주목할 것: 이 사고에서 **아무도 비난받지 않았습니다** — 원인이 '외부 변화 + 관측의 구멍'이라는
구조의 언어로 서술됐으니까요. 포스트모템 문서가 자동 생성되면 blameless는 문화가 아니라
**기본값**이 됩니다. 레코드는 사람을 탓할 줄 모르거든요.

## 🔬 연구 노트 — C9

1. **감지 지연 단축**: 방지책 1(주간 골든 자동 실행)을 실제로 구현해 같은 드리프트 주입 시 감지가 Day15+1로 앞당겨지는지 확인하라.
2. **트레이드오프 대장**: 임시 조치(문턱 3/3)의 재현율 -10%를 언제까지 감수할 것인가 — '임시 조치의 만료일'을 대장에 기록하는 관행을 설계하라 (임시가 영구가 되는 것도 좀비다).
3. **모델 버전 감지**: chat 응답에서 모델 식별 정보를 레코드에 남기고, 변화 시 골든 트리거를 거는 구조를 스케치하라 — 드리프트 범인 ②의 조기 경보.

> 🎯 **(전부 잊어도 이것만)**
> ## 사고의 날, 사람의 일은 **판단 2개**(임시 조치 결정·포스트모템 확정)뿐 — 경보·분류·재현·타임라인·박제는 지어둔 부품이 한다.
> 임시 조치는 되돌리기 쉬운 다이얼부터, 방지책도 게이트를 통과해야 반영, 그리고 **임시에는 만료일을** — 임시가 영구가 되는 것도 좀비다.

📎 **다음 강 — C10. 운영 센터 완성 + 캡스톤 🎓**: 4주차 — 부품 전부를 하나의 운영 센터로 총조립하고, 30일 시뮬레이션 완주를 확인합니다. 그리고 3부작 대장정의 마지막 캡스톤.`,
    },
  ],

  quiz: {
    title: "고급3·C9강 — 배운 것 확인하기",
    questions: [
      {
        type: "multiple-choice",
        question: "재현 단계에서 '코드 diff 없음 + 지침 diff 없음 + 특정 기간부터 발생'이 가리키는 것은?",
        options: [
          "시스템에 유령이 산다",
          "내부는 그대로인데 결과가 변했다 — 범인은 외부(모델·세상·사용자)의 변화, 즉 드리프트",
          "레코드가 조작됐다",
        ],
        correctIndex: 1,
        explanation:
          "A1 드리프트 3범인의 소거법이에요. 내부 변화가 없음을 증명할 수 있는 것도 버전·레코드 기록 덕분입니다.",
      },
      {
        type: "multiple-choice",
        question: "사고 중 임시 조치로 '문턱 상향'(설정 다이얼)을 먼저 쓰는 이유는?",
        options: [
          "다이얼 돌리기가 재미있어서",
          "즉시 적용·즉시 복원이 가능한 되돌리기 쉬운 조치이기 때문 — 구조 변경은 포스트모템과 게이트를 거쳐서",
          "문턱이 높을수록 항상 좋아서",
        ],
        correctIndex: 1,
        explanation:
          "사고 중의 성급한 구조 변경은 이차 사고의 씨앗이에요. 다이얼로 지혈하고, 진짜 수술(방지책)은 게이트를 통과시켜 반영합니다. 단, 임시 조치에는 만료일을!",
      },
      {
        type: "multiple-choice",
        question: "포스트모템 자동 생성이 blameless(비난 없음)를 기본값으로 만드는 이유는?",
        options
: [
          "자동 문서에는 이름 쓰는 칸이 없어서",
          "타임라인·원인이 run 레코드라는 구조의 언어로 서술되므로 — 레코드는 사람을 탓할 줄 모른다",
          "AI가 착해서",
        ],
        correctIndex: 1,
        explanation:
          "기억 대결이 사라지면 비난도 설 자리를 잃어요. '누가'가 아니라 '무엇이'를 레코드가 이미 답해두기 때문입니다.",
      },
    ],
  } satisfies Quiz,
};
