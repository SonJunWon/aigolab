import type { Lesson } from "../../../types/lesson";
import type { Quiz } from "../../../types/quiz";

/**
 * W09 — 감정 분석 대시보드.
 *
 * Part A: 플랫폼에서 감정 분류·배치 분석·인사이트 생성 체험 (LLM 셀)
 * Part B: MD 레시피로 CSV 업로드 → 대시보드 웹앱 완성 (Claude Code / Cursor)
 */
export const workshopW09: Lesson = {
  id: "ai-eng-w09-sentiment-dashboard",
  language: "ai-engineering",
  track: "beginner",
  order: 109,
  title: "W09: 감정 분석 대시보드",
  subtitle: "리뷰 데이터를 AI로 분석하고 차트로 시각화",
  estimatedMinutes: 120,
  cells: [
    {
      type: "markdown",
      source: `# 📊 감정 분석 대시보드 만들기

> 💡 **보라색 점선 밑줄** 이 있는 단어는 전문 용어예요. **마우스를 올리면** (모바일은 터치) 쉬운 설명이 나옵니다.

## 이 워크샵에서 만들 것

**리뷰 CSV 파일을 업로드하면 AI 가 감정을 분류하고, 파이 차트·막대 차트·키워드 빈도·요약 카드로 시각화**하는 대시보드를 만듭니다.

### 완성 모습
\`\`\`
┌────────────────────────────────────────────┐
│  📊 감정 분석 대시보드                      │
├────────────────────────────────────────────┤
│                                            │
│  ┌──────────────────────────────────────┐  │
│  │  📂 CSV 파일을 여기에 놓거나          │  │
│  │     클릭해서 업로드                   │  │
│  └──────────────────────────────────────┘  │
│                                            │
│  [미리보기: 첫 10행 테이블]                 │
│  열 선택: [리뷰 텍스트 ▼]  [🔍 분석 시작]  │
│  ████████████░░░░░ 65% (13/20 완료)        │
│                                            │
│  ┌──────────┐  ┌───────────────────────┐  │
│  │ 🥧 감정   │  │ 📊 신뢰도 분포        │  │
│  │  긍정 45% │  │ ██████ 0.9+           │  │
│  │  부정 30% │  │ ████   0.7-0.9        │  │
│  │  중립 25% │  │ ██     0.5-0.7        │  │
│  └──────────┘  └───────────────────────┘  │
│                                            │
│  ┌──────────────────────────────────────┐  │
│  │ 🔤 키워드 빈도 (Top 20)              │  │
│  │ 친절(14) 배송(11) 맛있(9) 가격(8)... │  │
│  └──────────────────────────────────────┘  │
│                                            │
│  ┌──────────────────────────────────────┐  │
│  │ 💡 AI 인사이트 요약                   │  │
│  │ 주요 긍정: 친절한 서비스, 빠른 배송   │  │
│  │ 주요 불만: 포장 상태, 가격 인상       │  │
│  │ 개선 제안: 포장 품질 개선 권장...     │  │
│  └──────────────────────────────────────┘  │
│                                            │
│  [📥 CSV 내보내기] [🖼️ 이미지 저장]        │
└────────────────────────────────────────────┘
\`\`\`

### 2단계 진행

| 단계 | 내용 | 시간 | 장소 |
|---|---|---|---|
| **Part A** | ==감정 분석== 기초 체험 (분류·배치·인사이트) | 40분 | 이 페이지 (LLM 셀) |
| **Part B** | MD 레시피로 대시보드 웹앱 완성 | 80분 | 내 컴퓨터 (VS Code/Cursor) |

> ⚠️ **Part B 전제**: W00 "내 컴퓨터를 AI 작업실로" 완료 (Node.js + VS Code + Claude Code/Cursor 설치)`,
    },

    // ─── Part A: 감정 분석 체험 ───
    {
      type: "markdown",
      source: `## Part A: 감정 분석 체험 (40분)

==감정 분석== 이란 텍스트를 읽고 **긍정 / 부정 / 중립** 같은 감정을 자동으로 판별하는 기술이에요.
쇼핑몰 리뷰, SNS 댓글, 고객 문의 등을 분석할 때 많이 쓰입니다.

전통적으로는 머신러닝 모델을 직접 훈련해야 했지만,
LLM 을 쓰면 ==few-shot== 프롬프트 몇 줄로 바로 분류할 수 있어요!

> 💡 ==few-shot== = "예시 몇 개를 보여주고 패턴을 알려주는 것". 모델을 따로 훈련하지 않아도 됩니다.`,
    },

    {
      type: "markdown",
      source: `### A-1. 감정 분류 기초 — 리뷰 한 줄 분석하기

리뷰 한 줄을 AI 에게 주고 **긍정/부정/중립** 으로 분류해봅니다.
==few-shot== 예시를 시스템 프롬프트에 넣으면 정확도가 확 올라가요!`,
    },

    {
      type: "llm-code",
      language: "typescript",
      source: `// A-1: 감정 분류 기초 — few-shot 으로 리뷰 감정 분류
const review = "배송은 빨랐는데 포장이 좀 아쉬웠어요. 상품 자체는 괜찮습니다.";

const response = await chat({
  provider: "gemini",
  messages: [
    {
      role: "system",
      content: \`너는 감정 분석 전문가야. 리뷰를 읽고 감정을 분류해.

## 분류 기준
- 긍정: 만족, 추천, 칭찬이 주된 내용
- 부정: 불만, 실망, 비판이 주된 내용
- 중립: 긍정과 부정이 섞여 있거나 객관적 서술

## 예시 (few-shot)
리뷰: "정말 맛있어요! 다음에 또 시킬게요"
→ 감정: 긍정 | 신뢰도: 0.95 | 키워드: 맛있, 재구매

리뷰: "배송이 일주일이나 걸렸고 상품도 불량이에요"
→ 감정: 부정 | 신뢰도: 0.92 | 키워드: 배송지연, 불량

리뷰: "가격 대비 보통이에요. 나쁘지는 않아요"
→ 감정: 중립 | 신뢰도: 0.80 | 키워드: 가격대비, 보통

## 출력 형식
반드시 아래 형식으로만 답해:
감정: [긍정/부정/중립] | 신뢰도: [0.0~1.0] | 키워드: [쉼표 구분]
이유: [한 줄 설명]\`,
    },
    { role: "user", content: \`이 리뷰를 분석해줘: "\${review}"\` },
  ],
});

console.log("📝 원본 리뷰:");
console.log(review);
console.log("\\n🔍 AI 분석 결과:");
console.log(response.text);`,
      hints: [
        "few-shot 예시를 3개 넣었더니 AI 가 형식을 잘 따라해요. 이게 few-shot 학습!",
        "신뢰도(confidence)는 AI 가 스스로 판단한 확신 정도예요.",
        "review 변수를 다른 리뷰로 바꿔서 실행해보세요!",
      ],
    },

    {
      type: "markdown",
      source: `### A-2. 배치 분석 — 여러 리뷰를 한꺼번에 분석

리뷰가 수십, 수백 개일 때는 하나씩 보내면 너무 느려요.
==배치 처리== 로 여러 리뷰를 한 번에 보내고 **JSON 배열** 로 결과를 받아봅시다.`,
    },

    {
      type: "llm-code",
      language: "typescript",
      source: `// A-2: 배치 감정 분석 — 여러 리뷰를 JSON 배열로 받기
const reviews = [
  "직원분이 너무 친절해서 기분 좋았어요!",
  "음식이 짜고 양도 적어서 실망했습니다.",
  "위치는 좋은데 주차가 불편해요.",
  "가격 대비 훌륭합니다. 강추!",
  "주문한 것과 다른 메뉴가 왔어요. 환불 요청했습니다.",
  "보통이에요. 특별한 건 없지만 나쁘지도 않아요.",
  "분위기가 아늑하고 디저트가 맛있어요 💕",
  "30분 넘게 기다렸는데 사과도 없었어요.",
];

const response = await chat({
  provider: "gemini",
  messages: [
    {
      role: "system",
      content: \`너는 감정 분석 API야. 리뷰 목록을 받으면 JSON 배열로만 답해.
다른 텍스트 없이 순수 JSON 만 출력해.

각 항목 형식:
{
  "text": "원본 리뷰 (앞 20자...)",
  "sentiment": "긍정" | "부정" | "중립",
  "confidence": 0.0 ~ 1.0,
  "keywords": ["키워드1", "키워드2"]
}\`,
    },
    {
      role: "user",
      content: \`아래 \${reviews.length}개 리뷰를 분석해줘:\\n\${reviews.map((r, i) => \`\${i + 1}. \${r}\`).join("\\n")}\`,
    },
  ],
});

console.log("📊 배치 분석 결과:");
console.log(response.text);

// JSON 파싱 + 통계
try {
  const cleaned = response.text.replace(/\`\`\`json?\\n?/g, "").replace(/\`\`\`/g, "").trim();
  const results = JSON.parse(cleaned);

  const counts = { 긍정: 0, 부정: 0, 중립: 0 };
  for (const r of results) {
    counts[r.sentiment as keyof typeof counts]++;
  }

  console.log("\\n── 감정 분포 ──");
  console.log(\`😊 긍정: \${counts["긍정"]}건 (\${Math.round((counts["긍정"] / results.length) * 100)}%)\`);
  console.log(\`😠 부정: \${counts["부정"]}건 (\${Math.round((counts["부정"] / results.length) * 100)}%)\`);
  console.log(\`😐 중립: \${counts["중립"]}건 (\${Math.round((counts["중립"] / results.length) * 100)}%)\`);

  // 전체 키워드 빈도
  const kwCount: Record<string, number> = {};
  for (const r of results) {
    for (const kw of r.keywords) {
      kwCount[kw] = (kwCount[kw] || 0) + 1;
    }
  }
  const topKw = Object.entries(kwCount)
    .sort((a, b) => b[1] - a[1])
    .slice(0, 5);
  console.log("\\n🔤 상위 키워드:", topKw.map(([k, v]) => \`\${k}(\${v})\`).join(", "));
} catch {
  console.log("⚠️ JSON 파싱 실패 — 다시 실행해보세요!");
}`,
      hints: [
        "8개 리뷰를 한 번에 보내서 JSON 배열로 받았어요. 이게 배치 처리!",
        "실제 앱에서는 수백 건을 10~20개씩 나눠서 보내고 진행률을 표시해요.",
        "키워드 빈도를 세면 '어떤 주제가 많이 언급되는지' 한눈에 파악할 수 있어요.",
      ],
    },

    {
      type: "markdown",
      source: `### A-3. 인사이트 생성 — 분석 결과로 보고서 만들기

숫자와 키워드만으로는 부족해요. AI 에게 **분석 결과를 종합**해서
사람이 읽기 좋은 인사이트 보고서를 만들어봅시다.`,
    },

    {
      type: "llm-code",
      language: "typescript",
      source: `// A-3: 분석 결과 → AI 인사이트 보고서 생성
// (A-2 의 분석 결과를 가정한 데이터)
const analysisData = {
  total: 8,
  sentiments: { 긍정: 3, 부정: 3, 중립: 2 },
  positiveKeywords: ["친절", "맛있", "강추", "분위기", "디저트"],
  negativeKeywords: ["짜다", "적다", "실망", "대기", "사과없음", "환불"],
  neutralKeywords: ["보통", "주차", "위치"],
};

const response = await chat({
  provider: "gemini",
  messages: [
    {
      role: "system",
      content: \`너는 비즈니스 인사이트 분석가야.
감정 분석 결과를 받으면 경영진이 읽기 좋은 보고서를 작성해.

보고서 형식:
## 📋 감정 분석 인사이트 보고서

### 전체 요약
(한 문단 — 전체적 감정 동향)

### 😊 주요 긍정 포인트
- (긍정 키워드 기반으로 3가지)

### 😠 주요 불만 사항
- (부정 키워드 기반으로 3가지)

### 💡 개선 제안
1. (가장 시급한 개선 사항)
2. (두 번째 개선 사항)
3. (세 번째 개선 사항)

### 🎯 액션 아이템
- (구체적 실행 항목 2-3개)

한국어로, 간결하게 작성해.\`,
    },
    {
      role: "user",
      content: \`아래 감정 분석 결과로 인사이트 보고서를 작성해줘:

총 리뷰 수: \${analysisData.total}건
감정 분포: 긍정 \${analysisData.sentiments["긍정"]}건, 부정 \${analysisData.sentiments["부정"]}건, 중립 \${analysisData.sentiments["중립"]}건
긍정 키워드: \${analysisData.positiveKeywords.join(", ")}
부정 키워드: \${analysisData.negativeKeywords.join(", ")}
중립 키워드: \${analysisData.neutralKeywords.join(", ")}\`,
    },
  ],
});

console.log(response.text);`,
      hints: [
        "숫자 + 키워드를 AI 에게 넘기면 사람이 읽기 좋은 보고서가 나와요!",
        "실제 대시보드에서 이 보고서를 '인사이트 카드' 에 표시하면 됩니다.",
        "analysisData 를 Part B 앱의 실제 분석 결과로 바꾸면 진짜 보고서!",
      ],
    },

    {
      type: "markdown",
      source: `## ✅ Part A 완료!

==감정 분석== 의 핵심 3단계를 체험했어요:
- ✅ ==few-shot== 프롬프트로 리뷰 감정 분류 (긍정/부정/중립)
- ✅ ==배치 처리== 로 여러 리뷰 한꺼번에 분석 + JSON 파싱
- ✅ 분석 결과 → AI 인사이트 보고서 자동 생성

이제 Part B 에서 **CSV 업로드 → 차트 시각화 → 인사이트 요약** 까지 되는 대시보드 앱을 만듭니다!

> 💡 Part B 의 핵심 기술: Papa Parse (CSV 파싱) + Chart.js (차트) + Gemini (감정 분류)

---

## Part B: MD 레시피로 대시보드 완성 (80분)

여기서부터는 **내 컴퓨터에서** 진행합니다.

### 바이브 코딩 플로우

\`\`\`
[MD 레시피 파일] → [Claude Code / Cursor] → [완성된 감정 분석 대시보드]
\`\`\`

아래의 **MD 레시피** 를 복사해서 AI 코딩 도구에 전달하면 됩니다.`,
    },

    {
      type: "markdown",
      source: `### 📋 MD 레시피 — 복사해서 사용하세요

아래 마크다운 전체를 복사해 **Claude Code** 에 붙여넣거나, **Cursor** 의 AI 채팅에 전달하세요.

---

\`\`\`markdown
# 감정 분석 대시보드 웹앱 제작 요청서

## 프로젝트 개요
CSV 파일(리뷰 데이터)을 업로드하면 AI 가 감정을 분류하고,
파이 차트·막대 차트·키워드 빈도·요약 카드로 시각화하는 대시보드를 만들어주세요.
Google Gemini 2.5 Flash 를 감정 분류 엔진으로 사용합니다.

## 기술 스택
- React + TypeScript + Vite
- Tailwind CSS (다크/라이트 모드)
- @google/genai (Gemini SDK)
- Chart.js + react-chartjs-2 (파이 차트, 막대 차트)
- PapaParse (CSV 파싱)
- html2canvas (대시보드 이미지 내보내기)

## 기능 요구사항

### 1. CSV 파일 업로드
- 드래그&드롭 존 + 클릭 업로드 (accept=".csv")
  - 점선 테두리, 아이콘 + "CSV 파일을 여기에 놓거나 클릭해서 업로드" 텍스트
  - 드래그 오버 시 테두리 색상 변경 + 배경 하이라이트
- PapaParse 로 CSV 파싱 (header: true, encoding: UTF-8)
- 파싱 완료 후 "○○건 로드됨" 표시
- **샘플 데이터** 버튼: 클릭하면 한국어 쇼핑몰 리뷰 30건 데모 CSV 자동 로드
  (긍정 13건, 부정 10건, 중립 7건 정도로 구성)

### 2. 데이터 미리보기
- 테이블로 첫 10행 표시 (스크롤 가능)
- 각 열 이름 표시
- **열 선택 드롭다운**: "어떤 열이 리뷰 텍스트인가요?" — 선택하면 해당 열을 분석 대상으로 지정
- 총 행 수 표시

### 3. 분석 실행
- [🔍 분석 시작] 버튼
- 리뷰를 10개씩 배치로 Gemini 에 전송
- 프로그레스 바: "분석 중... 13/30 완료 (43%)"
- 각 리뷰에 대해 JSON 으로 결과 수신:
  \\\`\\\`\\\`json
  {
    "text": "원본 리뷰 (앞 30자...)",
    "sentiment": "긍정" | "부정" | "중립",
    "confidence": 0.0 ~ 1.0,
    "keywords": ["키워드1", "키워드2"]
  }
  \\\`\\\`\\\`
- Gemini 호출 시 few-shot 예시 3개를 system prompt 에 포함
- 분석 완료 후 대시보드 자동 표시

### 4. 대시보드 패널 (2x2 그리드 + 하단 카드)

#### 4-1. 파이 차트: 감정 비율
- 긍정(초록) / 부정(빨강) / 중립(회색) 세 조각
- 각 조각에 퍼센트 레이블
- **클릭 인터랙션**: 파이 조각 클릭 → 하단 테이블이 해당 감정만 필터링

#### 4-2. 막대 차트: 신뢰도 분포
- X 축: 신뢰도 구간 (0.5 미만, 0.5-0.7, 0.7-0.9, 0.9 이상)
- Y 축: 리뷰 수
- 각 막대에 수치 레이블

#### 4-3. 키워드 빈도 (Top 20)
- 모든 리뷰에서 추출된 키워드를 빈도순 정렬
- 태그 칩 형태로 표시 (크기 = 빈도에 비례)
- 긍정 리뷰 키워드는 초록, 부정은 빨강, 중립은 회색 배경

#### 4-4. AI 인사이트 요약 카드
- 분석 완료 후 Gemini 에 결과를 보내서 인사이트 보고서 생성
- 주요 긍정 포인트 / 주요 불만 사항 / 개선 제안 표시
- 마크다운 형식으로 렌더링

### 5. 결과 테이블
- 모든 리뷰 + 분석 결과를 테이블로 표시
  - 열: 번호, 리뷰 텍스트, 감정, 신뢰도, 키워드
  - 감정별 색상 배지 (긍정=초록, 부정=빨강, 중립=회색)
- 파이 차트 조각 클릭 시 해당 감정만 필터링
- 정렬: 신뢰도순, 감정순 토글

### 6. 내보내기
- [📥 CSV 내보내기]: 원본 리뷰 + 감정 + 신뢰도 + 키워드를 CSV 로 다운로드
- [🖼️ 이미지 저장]: html2canvas 로 대시보드 영역을 PNG 로 캡처

### 7. API 키 관리
- 첫 실행 시 API 키 입력 모달:
  - "🔑 Gemini API 키를 입력하세요" 제목
  - 입력란 (type=password, 토글로 보이기/숨기기)
  - "https://aistudio.google.com/apikey 에서 무료 발급" 안내 링크
  - [저장] 버튼
- localStorage 에 키 저장
- 상단 ⚙️ 버튼으로 키 변경/삭제

### 8. 디자인
- 다크 모드 기본 (배경: #0f172a, 카드: #1e293b, 텍스트: #e2e8f0)
- 라이트 모드 (배경: #f8fafc, 카드: white, 텍스트: #1e293b)
- 다크/라이트 모드 토글 (상단 헤더에 🌙/☀️)
- 반응형: 모바일 세로 1열, 데스크탑 2x2 그리드
- 차트 카드에 그림자 + 둥근 모서리
- 프로그레스 바 애니메이션

### 9. 샘플 데이터 (앱 내 하드코딩)
앱에 데모용 한국어 리뷰 30건을 내장해주세요:
- 쇼핑몰 리뷰 느낌 (배송, 품질, 가격, 서비스 등)
- 긍정 13건, 부정 10건, 중립 7건 정도 비율
- 사용자가 [📦 샘플 데이터 로드] 버튼 누르면 CSV 업로드 없이 바로 분석 시작 가능

## 환경 변수
GEMINI_API_KEY 는 런타임에 사용자가 입력 (하드코딩 금지)

## 실행 방법
npm install → npm run dev → localhost:5173 에서 확인

## 주의사항
- API 키를 소스 코드에 절대 하드코딩하지 마세요
- 모든 텍스트는 한국어로
- 배치 전송 시 10건씩 나눠서 호출 (Rate limit 방지)
- JSON 파싱 실패 시 해당 리뷰는 "분석 실패" 로 표시하고 건너뜀
- TypeScript strict 모드
- Chart.js 의 register 를 잊지 마세요 (ArcElement, CategoryScale 등)
\`\`\`

---

### 사용 방법

**🅰️ Claude Code 사용자:**
\`\`\`bash
mkdir sentiment-dashboard && cd sentiment-dashboard
claude
# 위 마크다운 전체를 붙여넣기
# Claude 가 파일 생성 제안 → Accept
# claude 에서 나온 후:
npm install
npm run dev
\`\`\`

**🅱️ Cursor 사용자:**
1. 새 폴더 열기 → 터미널에서 \`npm init -y\`
2. **Cmd+I** → 위 마크다운 붙여넣기
3. Cursor 가 생성한 파일들 승인
4. 터미널: \`npm install && npm run dev\`

### 💡 커스텀 팁
- "시간대별 감정 트렌드 차트 추가해줘" — 날짜 열이 있으면 시계열 분석!
- "워드클라우드 라이브러리로 키워드 시각화해줘" — react-wordcloud 패키지 추가
- "감정별로 대표 리뷰 3개씩 하이라이트해줘" — 가장 신뢰도 높은 리뷰 선별
- "영어 리뷰도 분석할 수 있게 해줘" — 다국어 프롬프트 확장

> 🚨 **안 되면**: AI 에게 에러 메시지를 그대로 붙여넣으세요.
> "이 에러가 나와요: [에러 내용]" → AI 가 수정해줍니다. 이게 바이브 코딩!`,
    },

    {
      type: "markdown",
      source: `## 🎯 도전 과제

기본 대시보드가 완성되면 MD 레시피에 아래를 추가해서 AI 에게 요청해보세요:

| 난이도 | 추가 기능 |
|---|---|
| ⭐ | 감정별 대표 리뷰 Top 3 하이라이트 |
| ⭐ | 분석 결과 PDF 보고서 다운로드 |
| ⭐⭐ | 워드클라우드 시각화 (react-wordcloud) |
| ⭐⭐ | 시간대별 감정 트렌드 라인 차트 |
| ⭐⭐⭐ | 다국어 감정 분석 (한국어 + 영어 + 일본어) |
| ⭐⭐⭐ | 실시간 스트리밍: 리뷰 하나씩 분석되며 차트가 실시간 업데이트 |

추가할 때마다 AI 에게:
> "기존 코드에 [기능] 을 추가해줘"

라고 하면 됩니다. **이게 바이브 코딩의 핵심 — 대화하듯 앱을 키워가기.**

## ✅ W09 완료!

- ✅ Part A: ==few-shot== 감정 분류 + ==배치 처리== + 인사이트 생성
- ✅ Part B: MD 레시피 → CSV 업로드 감정 분석 대시보드 완성
- ✅ 도전: AI 에게 기능 추가 요청

### 다음 워크샵 예고
**W10: AI 이메일 비서** — 받은 이메일을 AI 가 분류하고, 답장 초안을 자동 생성하고,
우선순위를 매겨주는 스마트 이메일 관리 앱을 만듭니다!`,
    },
  ],

  quiz: {
    title: "W09 — 감정 분석 대시보드 확인",
    questions: [
      {
        type: "multiple-choice",
        question:
          "few-shot 학습이란 무엇인가요?",
        options: [
          "AI 모델을 처음부터 훈련시키는 것",
          "프롬프트에 예시 몇 개를 넣어서 AI 가 패턴을 따라하게 하는 것",
          "데이터를 아주 적게 사용해서 학습하는 딥러닝 기법",
          "AI 에게 한 번만 질문하는 것",
        ],
        correctIndex: 1,
        explanation:
          "few-shot = 프롬프트 안에 예시(shot)를 몇 개 넣어서 AI 가 형식과 패턴을 이해하게 하는 기법이에요. 모델을 따로 훈련하지 않아도 돼요!",
      },
      {
        type: "multiple-choice",
        question:
          "리뷰 200개를 감정 분석할 때, 배치 처리를 하는 이유는?",
        options: [
          "한 번에 200개를 보내면 더 정확해서",
          "API 호출 횟수를 줄이고 속도를 높이기 위해",
          "배치 처리가 더 저렴해서",
          "AI 가 200개를 한꺼번에 분석하는 걸 더 좋아해서",
        ],
        correctIndex: 1,
        explanation:
          "200개를 하나씩 보내면 API 호출 200번! 10개씩 배치로 보내면 20번이면 끝나요. 속도도 빠르고 rate limit 도 피할 수 있어요.",
      },
      {
        type: "multiple-choice",
        question:
          "감정 분석 대시보드에서 파이 차트의 한 조각을 클릭하면 어떤 일이 일어나나요?",
        options: [
          "차트가 확대된다",
          "해당 감정의 리뷰만 테이블에 필터링되어 보인다",
          "해당 감정의 리뷰가 삭제된다",
          "새로운 분석이 시작된다",
        ],
        correctIndex: 1,
        explanation:
          "파이 차트의 '긍정' 조각을 클릭하면 하단 테이블에 긍정 리뷰만 필터링! 인터랙티브 대시보드의 핵심 기능이에요.",
      },
    ],
  } satisfies Quiz,
};
