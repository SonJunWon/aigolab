import type { Lesson } from "../../../types/lesson";
import type { Quiz } from "../../../types/quiz";

/**
 * W10 — AI 이메일 비서.
 *
 * Part A: 플랫폼에서 이메일 생성/톤 변환/개선 체험 (LLM 셀)
 * Part B: MD 레시피로 이메일 비서 웹앱 완성 (Claude Code / Cursor)
 */
export const workshopW10: Lesson = {
  id: "ai-eng-w10-email-assistant",
  language: "ai-engineering",
  track: "beginner",
  order: 110,
  title: "W10: AI 이메일 비서",
  subtitle: "용도와 톤만 선택하면 완벽한 이메일 초안 완성",
  estimatedMinutes: 90,
  cells: [
    {
      type: "markdown",
      source: `# 📧 AI 이메일 비서 만들기

> 💡 **보라색 점선 밑줄** 이 있는 단어는 전문 용어예요. **마우스를 올리면** (모바일은 터치) 쉬운 설명이 나옵니다.

## 이 워크샵에서 만들 것

**용도와 톤만 고르면 이메일 초안이 뚝딱 나오는 웹앱** — 핵심 내용을 적으면 AI 가 제목과 본문을 완성해주고, ==톤==을 바꾸거나 다듬기도 한 번에 됩니다.

### 완성 모습
\`\`\`
┌──────────────────────────────────┐
│  📧 AI 이메일 비서               │
├──────────────────────────────────┤
│                                  │
│  📌 용도 선택                    │
│  [업무 요청] [회의 초대] [감사]   │
│  [사과] [문의] [보고] [제안] [거절]│
│                                  │
│  🎭 톤 선택                      │
│  (●) 격식체  ( ) 비즈니스        │
│  ( ) 캐주얼  ( ) 친근한          │
│                                  │
│  📝 핵심 내용 (키포인트)          │
│  ┌────────────────────────────┐  │
│  │ - 프로젝트 일정 2주 연장 요청 │  │
│  │ - 인력 부족이 원인          │  │
│  │ - 대안으로 외주 투입 제안    │  │
│  └────────────────────────────┘  │
│                                  │
│  [✨ 이메일 생성]                 │
│                                  │
│  ── 생성 결과 ──                 │
│  📌 제목: 프로젝트 일정 조정 요청│
│  ──────────────────────────────  │
│  김부장님께,                     │
│                                  │
│  안녕하세요. 현재 진행 중인...    │
│  ...검토 부탁드리겠습니다.        │
│                                  │
│  감사합니다.                     │
│  홍길동 드림                     │
│                                  │
│  [📋 복사] [🔄 재생성] [🎭 톤변환]│
│  [📏 짧게] [📏 길게]             │
│  [💾 템플릿 저장] [📂 불러오기]   │
└──────────────────────────────────┘
\`\`\`

### 2단계 진행

| 단계 | 내용 | 시간 | 장소 |
|---|---|---|---|
| **Part A** | 이메일 생성 핵심 로직 체험 | 30분 | 이 페이지 (LLM 셀) |
| **Part B** | MD 레시피로 웹앱 완성 | 60분 | 내 컴퓨터 (VS Code/Cursor) |

> ⚠️ **Part B 전제**: W00 "내 컴퓨터를 AI 작업실로" 완료 (Node.js + VS Code + Claude Code/Cursor 설치)`,
    },

    // ─── Part A: 이메일 생성 핵심 로직 ───
    {
      type: "markdown",
      source: `## Part A: 이메일 생성 핵심 로직 (30분)

이메일 비서의 핵심은 3가지:
1. **용도 + 키포인트 → 초안 생성** — ==system prompt== 로 이메일 전문가 역할 부여
2. **==톤== 변환** — 같은 내용을 격식체/반말/비즈니스/친근한 스타일로
3. **다듬기** — 어색한 문장을 매끄럽게 개선

LLM 셀에서 하나씩 실험합니다.`,
    },

    {
      type: "markdown",
      source: `### A-1. 기본 이메일 생성 — 용도와 키포인트만 주면 끝

용도(업무 요청, 감사 등)와 전달할 핵심 내용을 주면, AI 가 제목 + 본문을 완성합니다.`,
    },

    {
      type: "llm-code",
      language: "typescript",
      source: `// 기본 이메일 생성 — 용도 + 키포인트 → 제목 + 본문
const purpose = "업무 요청";
const keyPoints = [
  "프로젝트 일정을 2주 연장 요청",
  "인력 부족이 주요 원인",
  "대안으로 외주 인력 투입 제안",
];
const recipientName = "김부장님";

const response = await chat({
  provider: "gemini",
  messages: [
    {
      role: "system",
      content: \`너는 비즈니스 이메일 전문 작성가야. 다음 규칙을 따라:
1. 반드시 "제목:" 으로 시작하는 한 줄 + 빈 줄 + 본문 형식
2. 한국어 비즈니스 이메일 관례를 따라 (인사 → 본문 → 마무리)
3. 핵심 내용을 빠짐없이 자연스럽게 녹여내
4. 간결하되 정중하게 작성\`,
    },
    {
      role: "user",
      content: \`다음 조건으로 이메일을 작성해줘:
- 용도: \${purpose}
- 받는 사람: \${recipientName}
- 핵심 내용:
\${keyPoints.map(p => "  • " + p).join("\\n")}\`,
    },
  ],
});

console.log("📧 생성된 이메일:\\n");
console.log(response.text);`,
      hints: [
        "system prompt 가 '이메일 전문가' 역할 — 형식과 규칙을 지정해요.",
        "keyPoints 배열을 바꿔보세요. '회의 초대', '감사 인사' 등 다른 용도도 시도!",
        "받는 사람 이름이 있으면 더 자연스러운 이메일이 나와요.",
      ],
    },

    {
      type: "markdown",
      source: `### A-2. 톤 변환 — 같은 내용, 다른 느낌

같은 이메일을 **격식체 / 비즈니스 / 캐주얼 / 친근한** 4가지 ==톤==으로 바꿔봅니다. 이메일의 분위기가 확 달라져요!`,
    },

    {
      type: "llm-code",
      language: "typescript",
      source: `// 톤 변환 — 같은 내용을 4가지 스타일로
const originalContent = "프로젝트 일정을 2주 연장해주셨으면 합니다. 인력 부족이 원인이며, 외주 투입을 대안으로 제안합니다.";

const tones = [
  { name: "격식체", desc: "매우 정중하고 공식적인 문어체. '~하겠습니다', '~바랍니다' 사용." },
  { name: "비즈니스", desc: "전문적이지만 자연스러운 톤. '~드립니다', '~부탁드립니다' 사용." },
  { name: "캐주얼", desc: "편안한 직장 동료 느낌. '~해요', '~할게요' 사용." },
  { name: "친근한", desc: "친한 선후배 사이. 반말 OK, 이모지 약간 사용." },
];

for (const tone of tones) {
  const res = await chat({
    provider: "gemini",
    messages: [
      {
        role: "system",
        content: \`너는 이메일 톤 변환 전문가야. 주어진 내용을 지정된 톤으로 이메일 형태로 다시 써줘. 제목도 포함.\`,
      },
      {
        role: "user",
        content: \`다음 내용을 "\${tone.name}" 톤으로 이메일을 작성해줘.
톤 설명: \${tone.desc}

원본 내용: \${originalContent}\`,
      },
    ],
  });

  console.log(\`── 🎭 \${tone.name} ──\`);
  console.log(res.text);
  console.log();
}`,
      hints: [
        "같은 '일정 연장' 요청인데 톤에 따라 완전히 다른 이메일이 나와요.",
        "격식체는 공문서 느낌, 친근한은 슬랙 메시지 느낌 — 상황에 맞게 고르면 돼요.",
        "실제 앱에서는 라디오 버튼으로 톤을 선택하게 만들 거예요.",
      ],
    },

    {
      type: "markdown",
      source: `### A-3. 이메일 개선 — 어색한 초안을 매끄럽게

직접 쓴 이메일이 어색할 때, AI 에게 맡기면 문법/구조/정중함을 한 번에 다듬어줍니다.`,
    },

    {
      type: "llm-code",
      language: "typescript",
      source: `// 이메일 개선 — 어색한 초안 → 매끄러운 이메일
const roughDraft = \`김팀장님

저 이번 프로젝트 일정 좀 늦어질거같은데요
사람이 부족해서 그런건데 2주만 더 주시면 안될까요?
외주를 쓰는것도 방법일것같습니다
확인좀 부탁드려요\`;

const res = await chat({
  provider: "gemini",
  messages: [
    {
      role: "system",
      content: \`너는 한국어 이메일 교정 전문가야. 다음을 개선해:
1. 맞춤법과 띄어쓰기 교정
2. 비즈니스 이메일 구조로 재구성 (인사 → 배경 → 요청 → 마무리)
3. 정중하지만 자연스러운 표현으로 다듬기
4. 제목 추가
5. 원본의 핵심 내용은 절대 빠뜨리지 않기

개선 후 "📝 변경 사항:" 으로 무엇을 바꿨는지 간단히 정리해줘.\`,
    },
    {
      role: "user",
      content: \`이 이메일을 개선해줘:\\n\\n\${roughDraft}\`,
    },
  ],
});

console.log("── 원본 ──");
console.log(roughDraft);
console.log();
console.log("── ✨ 개선 결과 ──");
console.log(res.text);`,
      hints: [
        "roughDraft 를 여러분이 실제로 쓴 이메일로 바꿔보세요!",
        "맞춤법, 구조, 톤 — 3가지를 한 번에 잡아줘요.",
        "'변경 사항' 요약을 요청하면 뭐가 바뀌었는지 학습할 수 있어요.",
      ],
    },

    {
      type: "markdown",
      source: `## ✅ Part A 완료!

이메일 비서의 핵심 로직을 검증했어요:
- ✅ 용도 + 키포인트 → 이메일 초안 생성
- ✅ ==톤== 변환 (격식체/비즈니스/캐주얼/친근한)
- ✅ 어색한 초안 → 매끄러운 이메일로 개선

이제 이걸 **실제 웹앱** 으로 만듭니다.

---

## Part B: MD 레시피로 웹앱 완성 (60분)

여기서부터는 **내 컴퓨터에서** 진행합니다.

### 바이브 코딩 플로우

\`\`\`
[MD 레시피 파일] → [Claude Code / Cursor] → [완성된 이메일 비서 앱]
\`\`\`

아래의 **MD 레시피** 를 복사해서 AI 코딩 도구에 전달하면 됩니다.`,
    },

    {
      type: "markdown",
      source: `### 📋 MD 레시피 — 복사해서 사용하세요

아래 마크다운 전체를 복사해 **Claude Code** 에 붙여넣거나, **Cursor** 의 AI 채팅에 전달하세요.

---

\`\`\`markdown
# AI 이메일 비서 웹앱 제작 요청서

## 프로젝트 개요
용도와 톤을 선택하고 핵심 내용만 입력하면 완벽한 이메일 초안을 생성해주는 AI 이메일 비서 웹앱을 만들어주세요.
Google Gemini API 를 사용합니다.

## 기술 스택
- React + TypeScript + Vite
- Tailwind CSS (다크 모드 기본)
- @google/genai (Gemini SDK)

## 기능 요구사항

### 1. 용도 선택 카드 UI
- 8개의 용도 카드를 2x4 그리드로 배치 (모바일은 2x4)
- 각 카드에 이모지 아이콘 + 이름:
  - 📋 업무 요청 / 📅 회의 초대 / 🙏 감사
  - 🙇 사과 / ❓ 문의 / 📊 보고 / 💡 제안 / 🚫 거절
- 선택한 카드는 파란색 테두리(ring-2 ring-blue-500) + 배경 하이라이트
- 한 번에 하나만 선택 가능

### 2. 톤 선택
- 4개의 톤을 라디오 버튼 그룹으로 표시:
  - 🎩 격식체 — "매우 정중, 공식 문서 스타일"
  - 💼 비즈니스 — "전문적이지만 자연스러운"
  - 😊 캐주얼 — "편안한 동료 느낌"
  - 🤗 친근한 — "친한 사이, 이모지 사용"
- 기본값: 비즈니스

### 3. 핵심 내용 입력
- textarea (최소 3줄, 자동 확장)
- placeholder: "전달할 핵심 내용을 적어주세요 (줄바꿈으로 구분)"
- 예시 버튼: 클릭하면 선택한 용도에 맞는 예시 키포인트 자동 채움

### 4. 받는 사람 정보 (선택사항)
- 접을 수 있는 섹션 (기본 접힘)
- 이름, 직책, 관계(상사/동료/부하/외부) 입력 필드
- 입력하면 이메일에 자연스럽게 반영

### 5. 이메일 생성 결과
- [✨ 이메일 생성] 버튼 (용도 + 키포인트 필수, 없으면 비활성)
- 생성 중: 스켈레톤 애니메이션 + "AI가 이메일을 작성하고 있어요..."
- 결과 표시:
  - 제목 영역: 📌 아이콘 + 제목 텍스트 (볼드)
  - 구분선
  - 본문 영역: 마크다운 렌더링 (줄바꿈 유지)
- Gemini 2.5 Flash 모델 사용
- system prompt: "너는 비즈니스 이메일 전문 작성가야. 반드시 '제목:' 으로 시작하는 한 줄 + 빈 줄 + 본문 형식으로 작성해. 한국어 비즈니스 이메일 관례를 따르고, 핵심 내용을 빠짐없이 자연스럽게 녹여내."

### 6. 빠른 액션 버튼
- 결과 하단에 가로 배치:
  - 📋 복사 — 제목 + 본문을 클립보드에 복사 + "복사됨!" 토스트
  - 🔄 재생성 — 같은 조건으로 다시 생성
  - 🎭 톤 변환 — 현재 결과를 다른 톤으로 변환 (드롭다운 선택)
  - 📏 짧게 / 📏 길게 — 현재 이메일을 더 짧게 또는 길게 조정

### 7. 템플릿 시스템
- [💾 템플릿으로 저장] 버튼:
  - 모달: 템플릿 이름 + 카테고리(업무/개인/기타) 입력
  - 현재 설정(용도, 톤, 키포인트, 받는 사람 정보)을 localStorage 에 저장
- [📂 템플릿 불러오기] 버튼:
  - 저장된 템플릿 목록 모달 (카테고리별 필터)
  - 각 항목에 이름, 용도, 톤 표시 + [불러오기] [삭제] 버튼
  - 불러오기 시 모든 입력 필드에 값 복원

### 8. 이메일 히스토리
- 사이드바 또는 하단 탭으로 최근 생성한 이메일 20개 표시
- 각 항목: 제목 미리보기 + 용도 뱃지 + 생성 시간
- 클릭하면 결과 영역에 해당 이메일 표시
- localStorage 에 저장 (최대 20개, FIFO)

### 9. 답장 모드
- [↩️ 답장 모드] 토글 버튼
- 활성화 시: 받은 이메일 붙여넣기 textarea 표시
- 받은 이메일 + 답장 키포인트를 입력하면 AI 가 맥락에 맞는 답장 생성
- system prompt 에 "받은 이메일의 맥락을 이해하고 적절한 답장을 작성해" 추가

### 10. API 키 관리
- 첫 실행 시 세련된 API 키 입력 모달:
  - "🔑 Gemini API 키를 입력하세요" 제목
  - 입력란 (type=password, 토글로 보이기/숨기기)
  - "https://aistudio.google.com/apikey 에서 무료 발급" 안내 링크
  - [저장] 버튼
- localStorage 에 키 저장
- 상단 ⚙️ 버튼 → 키 변경 또는 삭제

### 11. 디자인
- 다크 모드 기본 (배경: #0f172a, 카드: #1e293b, 텍스트: #e2e8f0)
- 라이트 모드 (배경: #f8fafc, 카드: white, 텍스트: #1e293b)
- 다크/라이트 모드 토글 (상단 헤더에 🌙/☀️ 버튼)
- 최대 너비 800px, 중앙 정렬
- 반응형: 모바일 세로 → 데스크탑 가로 레이아웃
- 부드러운 전환 애니메이션 (결과 등장 시 fade-in)
- 용도 카드 호버 시 살짝 올라오는 효과 (transform + shadow)

## 환경 변수
GEMINI_API_KEY 는 런타임에 사용자가 입력 (하드코딩 금지)

## 실행 방법
npm install → npm run dev → localhost:5173 에서 확인

## 주의사항
- API 키를 소스 코드에 절대 하드코딩하지 마세요
- 모든 텍스트는 한국어로
- console.error 로 에러 로깅
- TypeScript strict 모드
\`\`\`

---

### 사용 방법

**🅰️ Claude Code 사용자:**
\`\`\`bash
mkdir my-email-assistant && cd my-email-assistant
claude
# 위 마크다운 전체를 붙여넣기
# Claude 가 파일 생성 제안 → Accept
# claude 에서 나온 후:
npm install
npm run dev
\`\`\`

**🅱️ Cursor 사용자:**
1. 새 폴더 열기 → 터미널에서 \`npm init -y\`
2. **Cmd+I** → 위 마크다운 붙여넣기
3. Cursor 가 생성한 파일들 승인
4. 터미널: \`npm install && npm run dev\`

### 💡 커스텀 팁
- ==system prompt== 에 회사 이름/서명 추가 → 매번 자동 포함
- ==템플릿== 에 자주 쓰는 이메일 패턴 저장 → 원클릭 생성
- "영어 이메일 모드" 추가 요청 → 글로벌 업무에도 활용
- "맞춤법 검사 결과 표시" 추가 요청 가능

> 🚨 **안 되면**: AI 에게 에러 메시지를 그대로 붙여넣으세요.
> "이 에러가 나와요: [에러 내용]" → AI 가 수정해줍니다. 이게 바이브 코딩!`,
    },

    {
      type: "markdown",
      source: `## 🎯 도전 과제

기본 이메일 비서가 완성되면 MD 레시피에 아래를 추가해서 AI 에게 요청해보세요:

| 난이도 | 추가 기능 |
|---|---|
| ⭐ | 이메일 본문 + 제목 한 번에 복사 (서식 유지) |
| ⭐ | 생성된 이메일 글자 수 / 예상 읽기 시간 표시 |
| ⭐⭐ | 영어 ↔ 한국어 번역 버튼 (생성된 이메일 즉시 번역) |
| ⭐⭐ | 이메일 히스토리 검색 + 카테고리 필터 |
| ⭐⭐⭐ | 여러 수신자에게 보낼 때 이름만 바꿔 대량 생성 (메일 머지) |
| ⭐⭐⭐ | 받은 이메일 분석 — 감정/긴급도/요청사항 자동 추출 |

추가할 때마다 AI 에게:
> "기존 코드에 [기능] 을 추가해줘"

라고 하면 됩니다. **이게 바이브 코딩의 핵심 — 대화하듯 앱을 키워가기.**

## ✅ W10 완료!

- ✅ Part A: 이메일 생성 핵심 (용도/톤/개선)
- ✅ Part B: MD 레시피 → 실제 이메일 비서 웹앱 완성
- ✅ 커스텀: AI 에게 기능 추가 요청

### 다음 워크샵 예고
**W11: AI 학습 카드 생성기** — 텍스트를 넣으면 플래시카드가 자동으로 만들어지는 학습 도우미.
어떤 주제든 카드로 만들어 반복 학습해보세요!`,
    },
  ],

  quiz: {
    title: "W10 — AI 이메일 비서 확인",
    questions: [
      {
        type: "multiple-choice",
        question:
          "이메일 비서에서 AI 가 '격식체'와 '캐주얼' 을 구분하게 만드는 핵심은?",
        options: [
          "모델을 두 개 사용한다",
          "system prompt 또는 user prompt 에 원하는 톤 스타일을 구체적으로 설명한다",
          "temperature 를 0 으로 설정한다",
          "영어로 번역해서 보낸다",
        ],
        correctIndex: 1,
        explanation:
          "==톤== 변환의 핵심은 프롬프트 안에 '격식체: ~하겠습니다 사용' 처럼 구체적 스타일 지시를 넣는 거예요. 같은 모델이 지시에 따라 다른 문체를 생성합니다.",
      },
      {
        type: "multiple-choice",
        question:
          "==템플릿== 시스템에서 저장할 때 localStorage 를 쓰는 이유는?",
        options: [
          "서버 없이 브라우저에 데이터를 영구 저장할 수 있어서",
          "속도가 가장 빠른 데이터베이스라서",
          "다른 사람과 자동으로 공유되서",
          "용량 제한이 없어서",
        ],
        correctIndex: 0,
        explanation:
          "localStorage 는 서버 없이 브라우저에 키-값 데이터를 저장할 수 있어요. 템플릿처럼 가벼운 설정 데이터에 딱 맞죠. 단, 약 5MB 제한이 있어요.",
      },
      {
        type: "multiple-choice",
        question:
          "이메일 생성 시 '제목:' 으로 시작하라고 ==system prompt== 에 규칙을 넣는 이유는?",
        options: [
          "AI 가 한국어를 더 잘 이해해서",
          "출력 형식을 고정해야 앱에서 제목과 본문을 분리(파싱)할 수 있어서",
          "토큰을 절약하려고",
          "보안을 위해서",
        ],
        correctIndex: 1,
        explanation:
          "앱에서 '제목'과 '본문'을 따로 보여주려면, AI 출력을 코드로 파싱해야 해요. 출력 형식을 규칙으로 정해두면 안정적으로 분리할 수 있습니다.",
      },
    ],
  } satisfies Quiz,
};
