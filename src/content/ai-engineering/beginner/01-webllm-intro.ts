import type { Lesson } from "../../../types/lesson";
import type { Quiz } from "../../../types/quiz";

/**
 * Ch01 — AI 가 브라우저에 산다 (입문자 확장판).
 *
 * 목표: 코딩/AI 경험이 전혀 없어도 20~30분 안에 "AI 와 대화하는 코드" 를 이해하고
 * 작성할 수 있게 한다. 모든 용어를 처음 등장할 때 풀어 설명하고, 셀 실행 전후에
 * "무엇을 하는가 / 무엇을 관찰했나" 를 명시한다.
 *
 * 실행: WebLLM (Llama 3.2 1B, 약 879MB). 키·네트워크 불필요.
 * 의무: Meta Llama 라이선스 — "Built with Llama" 배지 + 라이선스 링크.
 */
export const lesson01: Lesson = {
  id: "ai-eng-01-webllm-intro",
  language: "ai-engineering",
  track: "beginner",
  order: 1,
  title: "AI 가 브라우저에 산다",
  subtitle: "설치도, 가입도 없이 — 지금 여기서 LLM 과 대화하기",
  estimatedMinutes: 30,
  cells: [
    // ─────────────────────────────────────────────
    // 1. 오리엔테이션
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `# 🤖 AI 가 여러분 브라우저에 산다

여러분은 지금 이 순간, **AI ==모델==을 여러분의 컴퓨터에서 직접 실행** 하게 됩니다.
- ❌ 서버 가입
- ❌ ==API 키== 발급
- ❌ 결제 정보 입력
- ✅ 그냥 실행

> 💡 **보라색 점선 밑줄** 이 있는 단어는 전문 용어예요. **마우스를 올리면** (모바일은 터치) 쉬운 설명이 나옵니다.

## 이 레슨에서 여러분이 배울 것

| # | 내용 | 왜 중요한가 |
|---|---|---|
| 1 | AI ==모델==을 브라우저에서 실행하는 **구조** | AI 는 이제 "특별한 서버" 만의 것이 아니에요 |
| 2 | \`chat()\` 함수로 AI 에게 **메시지 보내기** | 이 한 줄이 ==LLM== 앱 개발의 모든 시작점 |
| 3 | **응답 객체** (text, model, latency 등) 읽기 | 자동화·가공의 기본 |
| 4 | **==temperature==** 로 답변 "창의성" 조절 | 분류·창작에 맞는 설정 선택 |
| 5 | **==system prompt==** 로 AI 에게 "역할" 부여 | 같은 질문도 완전 다른 답을 받아내기 |
| 6 | **==모델== 크기의 한계** 를 직접 체감 | 왜 클라우드 ==LLM== 이 여전히 필요한지 이해 |

한 줄로 요약하면: **"AI 에게 말을 거는 방법" 의 기초** 를 실제 코드로 배웁니다.`,
    },

    // ─────────────────────────────────────────────
    // 2. 핵심 용어 사전
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 📚 먼저, 이 단어들만 알고 가요

AI 분야에는 낯선 단어가 많아요. 하지만 딱 **5개** 만 익혀도 레슨 따라가는 데 충분합니다.

### 📘 LLM (Large Language Model, 대규모 언어 모델)
문장을 읽고 **다음에 올 단어를 예측** 하도록 수백만 권의 책·기사·코드로 훈련된 거대한 수학 함수예요.
ChatGPT, Gemini, Claude 모두 LLM 입니다.

### 📘 모델 (Model)
"훈련이 끝난 결과물" 한 덩어리. 파일로 치면 GB 단위. 우리가 곧 쓸 **Llama 3.2 1B** 도 모델 이름입니다.

### 📘 파라미터 (Parameter) / "1B", "70B"
모델 안의 **숫자 개수**. "1B = 10억 개", "70B = 700억 개" 를 뜻해요.
- **많을수록** 똑똑하고 많은 걸 기억하지만, 계산이 느리고 메모리를 많이 씀.
- **적을수록** 빠르고 가볍지만, 어려운 질문에 버벅거림.
- 우리 Ch01 의 **1B** 는 "브라우저에서도 돌 만큼 작다" 는 뜻이에요.

### 📘 토큰 (Token)
LLM 이 한 번에 읽고 쓰는 "**글자보다 조금 큰 단위**". 한국어 기준 대략 1~3자, 영어는 단어 하나.
- "안녕하세요" ≈ 토큰 3개
- "Hello" ≈ 토큰 1개
- \`max_tokens: 100\` 은 "100 토큰까지만 답해" 라는 뜻.

### 📘 Prompt (프롬프트)
여러분이 LLM 에게 주는 **입력 텍스트** 전체. 질문, 지시문, 예시가 다 포함돼요.
"좋은 프롬프트를 만드는 기술" 이 바로 **프롬프트 엔지니어링** 입니다 (Ch02).

---

다섯 개 용어를 한 문장으로 엮으면:
> "우리는 \`Llama 3.2 1B\` **모델** (**파라미터** 10억 개짜리 **LLM**) 에게
> **프롬프트** 를 주고, **토큰** 단위로 답을 받습니다."`,
    },

    // ─────────────────────────────────────────────
    // 3. 왜 브라우저에서?
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 🧭 왜 "브라우저에서" AI 가 돌아갈까요?

보통 AI 모델은 **거대한 서버** 에서 돌아갑니다 (OpenAI, Google 등이 운영하는 GPU 서버).
그런데 모델이 충분히 작다면? 여러분 노트북의 **GPU 를 브라우저가 직접 사용** 해서 돌릴 수 있어요.

이걸 가능하게 하는 기술이 **WebGPU** + **WebLLM** 입니다.

### 작동 원리 (3줄 요약)
1. 첫 실행: 모델 파일(약 879MB)을 네트워크로 다운받아 브라우저에 **캐시** 저장
2. 캐시된 모델을 **WebGPU** (그래픽카드 API) 로 로드
3. 이후엔 **네트워크 없이** 여러분 노트북이 직접 답을 계산

### 이 방식의 특징
| 좋은 점 | 부담 |
|---|---|
| 🆓 비용 0원 | 🕒 첫 다운로드 2~3분 |
| 🔒 데이터 외부 유출 없음 | 💾 879MB 저장 공간 사용 |
| ✈️ 오프라인 동작 (캐시 후) | 🐢 큰 모델(70B)은 못 돌림 |
| 🏷️ 무한 사용 (한도 없음) | 🖥️ WebGPU 미지원 브라우저는 안 됨 |

**Chrome, Edge, Opera 최신 버전** 은 WebGPU 를 지원합니다. Safari 도 최근 버전부터 지원.
모바일은 아이폰 15 Pro 이상 / 일부 안드로이드만 됩니다 (PC 권장).`,
    },

    // ─────────────────────────────────────────────
    // 4. 첫 실행 경고
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## ⏰ 잠깐! 첫 실행 **꼭** 읽고 시작하세요

첫 번째 \`chat()\` 호출은 다른 어떤 코드와도 다릅니다. **879MB 모델 다운로드** 를 해야 하기 때문이에요.

| 항목 | 값 |
|:---|:---|
| ⏱️ **첫 실행 소요 시간** | **🔴 2~3분** (인터넷 속도에 따라) |
| 📦 다운로드 크기 | 약 879 MB |
| 🔁 두 번째 실행부터 | 🟢 몇 초 (브라우저가 모델을 IndexedDB 에 캐시) |
| 🌐 네트워크 | 첫 번째만 필요, 이후 오프라인 가능 |
| 💻 필요 사양 | WebGPU 지원 브라우저 + 여유 RAM 4GB+ |

> ### 🚨 2~3분간 아무것도 안 나와 보여도 **중단하지 마세요**
> 셀 아래에 실시간 **진행률 바** 와 \`Fetching ... Loading ...\` 메시지가 흐를 거예요.
> - ❌ Wi-Fi 끊기 금지
> - ❌ 탭 닫기 금지 (다시 다운받아야 함)
> - ❌ 브라우저 창 최소화는 OK (백그라운드 다운로드 계속됨)

### 실행 중 보게 될 모습
1. \`📥 모델 다운로드 중\` + 퍼센트 바가 천천히 차오름 (가장 오래 걸리는 단계)
2. \`⚙️ 모델 로딩 중\` — WebGPU 에 모델 올리는 단계 (10~20초)
3. \`✅ 준비 완료\` — 이제 답이 출력됨
4. AI 가 생성한 텍스트가 콘솔에 나타남

💡 **기다리는 동안** 다음 셀 (마크다운 설명) 을 스크롤해서 미리 읽어두면 지루하지 않습니다.`,
    },

    // ─────────────────────────────────────────────
    // 5. 첫 셀 - 개념 설명
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 🚀 첫 대화 — 코드 해부

아래 셀을 실행하기 전에, 그 안의 코드가 **무슨 일을 하는지** 한 줄씩 살펴봅시다.

\`\`\`ts
const response = await chat({
  task: "offline",
  messages: [
    { role: "user", content: "파이썬으로 Hello World 를 출력하는 가장 짧은 방법은?" },
  ],
});
console.log(response.text);
\`\`\`

### 줄별 해설

**1줄: \`const response = await chat({ ... });\`**
- \`chat(...)\` — 우리 플랫폼이 제공하는 "AI 와 대화" 함수
- \`await\` — AI 응답은 시간이 걸려요. "기다렸다가 결과 받기" 를 뜻하는 TypeScript 키워드
- \`response\` — AI 의 답이 담긴 변수. 뒤에서 \`response.text\` 로 꺼내요

**2줄: \`task: "offline"\`**
- 이 레슨은 **WebLLM (브라우저 내)** 으로 돌리겠다는 지정
- Ch02 부터는 \`task: "fast"\` 나 \`provider: "gemini"\` 같은 다른 선택지를 써요

**3~5줄: \`messages: [ ... ]\`**
- AI 에게 보낼 **메시지 배열** (여러 턴의 대화 가능 — 지금은 한 턴)
- \`role: "user"\` — 사용자(여러분)의 발화
- \`content: "..."\` — 실제 질문/지시 텍스트

**6줄: \`console.log(response.text);\`**
- \`response.text\` — AI 가 생성한 답변 문자열
- \`console.log(...)\` — 셀 아래에 출력`,
    },

    // ─────────────────────────────────────────────
    // 6. 첫 LLM 셀
    // ─────────────────────────────────────────────
    {
      type: "llm-code",
      language: "typescript",
      source: `// task: "offline" 은 "네트워크·키 없이 브라우저 안에서" 라는 뜻입니다.
// 첫 실행은 879MB 모델 다운로드 때문에 2~3분 걸립니다. 진행률 바를 지켜보세요.
const response = await chat({
  task: "offline",
  messages: [
    { role: "user", content: "파이썬으로 Hello World 를 출력하는 가장 짧은 방법은?" },
  ],
});

console.log(response.text);`,
      hints: [
        "첫 실행은 모델 다운로드(879MB) 때문에 오래 걸려요. 진행률 바가 100% 가 될 때까지 기다려주세요.",
        "탭을 닫거나 Wi-Fi 를 끊으면 처음부터 다시 받아야 합니다.",
        "두 번째 실행부터는 몇 초 만에 완료돼요 (IndexedDB 캐시).",
      ],
    },

    // ─────────────────────────────────────────────
    // 7. 응답 객체 해설
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 🔍 방금 무슨 일이 있었나?

> ⚠️ **답변이 이상하거나 엉뚱한가요?** 정상입니다!
> 지금 사용 중인 모델은 **Llama 3.2 1B** — "1B" 는 **파라미터 10억 개** 라는 뜻이에요.
> 이 모델은 브라우저에서 돌릴 수 있을 만큼 **매우 작은 모델** 이라, 대형 모델(GPT-4, Gemini, Claude 등)에 비해 답변 품질이 낮습니다.
>
> **자주 나타나는 현상:**
> - 답변이 부정확하거나 엉뚱한 내용이 나옴
> - 같은 말을 반복하면서 응답이 매우 길어짐
> - 한국어가 어색하거나 영어가 섞임
>
> 이건 모델의 한계이지 여러분의 코드 문제가 아닙니다.
> Ch02 부터 사용할 **Gemini, Groq** 는 훨씬 정확하고 자연스러운 답변을 생성합니다.

### ==offline== 이란?

\`task: "offline"\` 은 **인터넷 없이, API 키 없이, 브라우저 안에서만** AI 를 돌리겠다는 뜻입니다.

| 비교 | offline (WebLLM) | fast (Gemini/Groq) |
|---|---|---|
| **실행 위치** | 내 브라우저 (로컬) | 클라우드 서버 |
| **인터넷 필요** | 모델 다운로드 때만 | 매번 필요 |
| **API 키** | 불필요 | 필요 (무료 발급) |
| **모델 크기** | 1B (작음) | 70B~수백B (큼) |
| **답변 품질** | 낮음 (실습용) | 높음 (실전용) |
| **속도** | 보통 | 빠름 |

Ch01 에서 offline 을 쓰는 이유: **API 키 없이도 바로 AI 를 체험** 할 수 있기 때문이에요.
Ch02 부터는 Gemini API 키를 등록하고 훨씬 똑똑한 모델을 사용합니다.

---

\`console.log(response.text)\` 만 출력했지만, \`response\` 변수에는 **답 외에도 여러 정보** 가 들어있어요.

응답 객체의 구조:
\`\`\`ts
{
  text: "파이썬으로 Hello World...",        // 답변 텍스트
  provider: "webllm",                      // 어느 제공자가 답했나
  model: "Llama-3.2-1B-Instruct-...",      // 정확한 모델 이름
  latencyMs: 12345,                        // 응답에 걸린 밀리초
  tokensUsed: {                            // 토큰 사용량
    input: 25,                             //   프롬프트에서 읽은 토큰
    output: 48,                            //   생성한 토큰
  },
  raw: { ... },                            // 원본 응답 (디버깅용)
}
\`\`\`

### 왜 이게 중요할까?
- **\`latencyMs\`** 로 "이 모델은 응답이 얼마나 빠른가" 비교
- **\`tokensUsed\`** 로 비용·한도 추적 (클라우드 LLM 유료 플랜에서 특히)
- **\`model\`** 로 "지금 돌아간 모델이 정확히 뭔가" 확인 — 버전 업그레이드 시 중요
- **\`provider\`** 로 "페일오버 됐는지" 확인 (Ch02 에서 다룸)

직접 확인하려면 아래 셀을 실행해보세요.`,
    },

    // ─────────────────────────────────────────────
    // 8. 응답 객체 실습
    // ─────────────────────────────────────────────
    {
      type: "llm-code",
      language: "typescript",
      source: `// 응답 객체의 여러 필드를 한눈에
const response = await chat({
  task: "offline",
  messages: [{ role: "user", content: "바나나는 왜 노란색일까?" }],
});

console.log("── 답변 ──");
console.log(response.text);

console.log("\\n── 메타 ──");
console.log("모델:", response.model);
console.log("응답 시간:", response.latencyMs, "ms");

if (response.tokensUsed) {
  console.log("입력 토큰:", response.tokensUsed.input);
  console.log("출력 토큰:", response.tokensUsed.output);
}`,
      hints: [
        "response 는 JavaScript 객체예요. 점(.) 으로 필드에 접근합니다.",
        "tokensUsed 는 provider 에 따라 없을 수도 있어 if 체크해요.",
        "console.log('\\n') 로 빈 줄을 넣어 출력을 깔끔하게.",
      ],
    },

    // ─────────────────────────────────────────────
    // 9. Temperature 개념 (확장)
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 🌡️ Temperature — AI 의 "창의성 다이얼"

**temperature** 는 LLM 의 가장 중요한 파라미터 중 하나예요. "같은 질문에 **얼마나 다양한 답** 을 할지" 를 정합니다.

### 0.0 ~ 1.0 (또는 2.0) 범위의 의미

| Temperature | 성격 | 같은 질문을 100번 물었을 때 |
|---|---|---|
| \`0.0\` | 🧊 결정적 | 거의 **모두 같은 답** |
| \`0.3\` | ❄️ 안정적 | 대부분 비슷, 살짝 변형 |
| \`0.7\` | 🌤️ 균형 | 뼈대는 같고 표현이 다양 |
| \`1.0\` | 🔥 창의적 | **꽤 다른 답** 이 많이 나옴 |
| \`>1.0\` | 💥 위험 | 의미 없는 말도 섞임 (특히 작은 모델) |

### 내부 동작 (호기심용)
LLM 은 매 토큰마다 "다음에 올 단어" 의 **확률 분포** 를 만들어요.
- \`temperature=0\` 은 **항상 가장 확률 높은 단어** 선택 → 결정적
- \`temperature=1\` 은 **확률대로 무작위 추출** → 다양
- \`temperature>1\` 은 **낮은 확률 단어도** 잘 뽑음 → 창의적이지만 부서지기 쉬움

### 🚨 스포일러 — 이어질 실험은 "일부러 엉망"

다음 셀에서 \`temperature=1.2\` 를 쓰면 결과가 **영어·한자·의미 없는 토큰** 범벅으로 나올 수 있어요.
이건 버그도 여러분 잘못도 아닙니다 — **1B 짜리 소형 모델의 본질적 한계** 예요.
확률 분포가 얕아서 한국어 같이 복잡한 언어는 \`temperature>1.0\` 에서 **쉽게 붕괴** 합니다.

이 "부서지는 순간" 을 **직접 보는 것** 이 이 실습의 핵심이에요. Ch02 에서 거대 모델로
같은 실험을 다시 해서 비교합니다.`,
    },

    // ─────────────────────────────────────────────
    // 10. Temperature 실습
    // ─────────────────────────────────────────────
    {
      type: "llm-code",
      language: "typescript",
      source: `// 짧은 과제 + 3단 temperature 비교 — 1B 모델이 어디서 부서지는지 관찰
const prompt = "고양이 이름을 한국어로 딱 하나만 지어줘. 이유 없이 이름만.";

const cold = await chat({
  task: "offline",
  temperature: 0.2,
  maxTokens: 40,
  messages: [{ role: "user", content: prompt }],
});

const warm = await chat({
  task: "offline",
  temperature: 0.7,
  maxTokens: 40,
  messages: [{ role: "user", content: prompt }],
});

const hot = await chat({
  task: "offline",
  temperature: 1.2,
  maxTokens: 40,
  messages: [{ role: "user", content: prompt }],
});

console.log("── 🟢 안전 (temp 0.2) ──");
console.log(cold.text);
console.log("\\n── 🟡 적당 (temp 0.7) ──");
console.log(warm.text);
console.log("\\n── 🔴 과열 (temp 1.2) ──");
console.log(hot.text);`,
      hints: [
        "maxTokens 로 출력 길이를 제한하면 부서져도 짧게 끝납니다. 안 쓰면 난장판이 길게 이어져요.",
        "같은 코드를 3~4번 실행해보세요. 0.2 는 거의 같은 답, 1.2 는 매번 다른 (그리고 이상한) 답을 얻습니다.",
        "temperature 를 0.0 으로 바꿔보세요. 실행마다 완전히 동일한 답이 나와요 (결정적).",
      ],
    },

    // ─────────────────────────────────────────────
    // 11. Temperature 해설
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 🎯 방금 본 것의 의미

실행한 결과를 관찰해보면:

- **🟢 0.2** 에서 나온 이름 — 여러 번 돌려도 거의 같은 답. "나비" / "하양이" 같은 보편적 선택.
- **🟡 0.7** 에서 — 약간 다양해지지만 **한국어 문법은 유지**. "별" / "모카" 등 여전히 사람이 지을 법한 이름.
- **🔴 1.2** 에서 — 한국어가 섞이거나, 영어/중국어가 침투하거나, 아예 의미 없는 토큰 나열.
  **이게 "모델이 부서졌다"** 는 상태.

### 💡 실무 temperature 가이드

| 작업 | 권장 temperature | 이유 |
|---|---|---|
| 데이터 분류 / 추출 | **0.0 ~ 0.2** | 일관성·재현성 최우선 |
| 코드 생성 / 번역 | **0.0 ~ 0.3** | 창의성보다 정확성 |
| 일반 챗봇 / Q&A | **0.3 ~ 0.7** | 자연스러우면서 일관적 |
| 카피라이팅 / 아이디에이션 | **0.7 ~ 1.0** | 다양성 유도 |
| **1.0 초과** | 일반적으로 비추천 | 특수한 탐색 실험용만 |

### 🔬 모델 크기의 영향
**작은 모델 (1B)** 일수록 이 "부서지는 경계" 가 낮아집니다.
- 1B : temp 1.0 이면 자주 부서짐
- 70B : temp 1.0 도 안정적
- Gemini (수천억 파라미터) : temp 1.5 도 잘 작동

Ch02 에서 **Gemini 2.5 Flash** 로 같은 실험을 해보면 temperature 1.0 에서도 한국어가 유지되는 걸
볼 수 있어요 — **모델 크기가 언어 안정성에 미치는 영향** 을 직관적으로 체감하게 됩니다.`,
    },

    // ─────────────────────────────────────────────
    // 12. System Prompt 개념
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 🛠️ System Prompt — AI 에게 "역할" 부여

지금까지는 \`role: "user"\` 메시지 하나만 보냈어요. 그런데 대화에는 **또 다른 role** 이 있습니다.

### Role 3종 세트

| role | 의미 | 예시 |
|---|---|---|
| \`system\` | AI 의 **성격·태도·제약** 을 미리 설정 | "너는 7살 아이야" |
| \`user\` | **사용자의 발화** (우리가 던지는 질문) | "왜 하늘은 파랄까?" |
| \`assistant\` | **AI 가 이전에 한 답** (멀티턴 대화에서 사용) | (자동으로 채워짐) |

### system 메시지의 힘

같은 질문 \`"왜 하늘은 파랄까?"\` 라도:
- system="너는 7살 아이야. 쉬운 말로 짧게."  → "빛이 파란색으로 더 많이 튕겨서 그래요!"
- system="너는 대학 물리 교수야."           → "레일리 산란 현상에 의한 것으로..."

**같은 질문, 완전히 다른 답**. 이게 프롬프트 엔지니어링의 출발점이에요.

### 실전 팁
- system 메시지는 **구체적일수록** 더 정확히 반영됨
- "짧게 답해", "존댓말로", "이모지 사용 금지" 같은 제약도 system 에 넣으면 잘 지킴
- 너무 길면 모델이 "지시를 잊음" — **한 단락 이내** 권장`,
    },

    // ─────────────────────────────────────────────
    // 13. System Prompt 실습
    // ─────────────────────────────────────────────
    {
      type: "llm-code",
      language: "typescript",
      source: `// temperature 를 낮게 + maxTokens 제한 = 1B 모델도 안정적으로 답변
// system 을 바꿔가며 실행해보세요. 완전히 다른 톤의 답이 나옵니다.
const response = await chat({
  task: "offline",
  temperature: 0.4,
  maxTokens: 120,
  messages: [
    {
      role: "system",
      content: "너는 7살 초등학생이야. 어려운 단어는 쓰지 말고 짧게 대답해.",
    },
    {
      role: "user",
      content: "왜 하늘은 파랄까?",
    },
  ],
});

console.log(response.text);`,
      hints: [
        "system 을 바꿔 실행해보세요: '너는 엄숙한 과학자야' / '너는 랩퍼야, 라임을 맞춰' / '너는 조선시대 선비야'.",
        "maxTokens 를 40 으로 줄이면 짧은 답만, 300 으로 늘리면 긴 설명을 얻습니다. 1B 모델이라 너무 길게 시키면 꼬여요.",
        "role 순서는 [system, user, (assistant, user)...] 로 user 로 끝나야 합니다.",
      ],
    },

    // ─────────────────────────────────────────────
    // 14. 멀티턴 대화 보너스
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 💬 보너스 — 멀티턴 대화

\`messages\` 배열에 여러 턴을 넣으면 AI 가 **이전 대화를 기억** 합니다.

\`\`\`ts
messages: [
  { role: "system", content: "너는 친절한 한국어 선생님이야." },
  { role: "user", content: "'가다' 의 과거형은?" },
  { role: "assistant", content: "갔다 입니다." },
  { role: "user", content: "그럼 부정 과거형은?" },  // ← 여기서 '가다' 문맥 유지
],
\`\`\`

마지막 질문의 "그럼" 이 무엇을 가리키는지 AI 가 안다는 뜻이에요.

> ⚠️ **주의**: LLM 은 "매번 전체 히스토리를 다시 읽어서" 답합니다.
> 대화가 길어질수록 토큰이 급증 → 비용·속도 부담. Ch06 에서 해결 기법을 배웁니다.`,
    },

    // ─────────────────────────────────────────────
    // 15. 문제 해결 가이드
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## ❗ 자주 만나는 문제와 해결

### 😵 "이 브라우저는 WebGPU 를 지원하지 않아..." 에러
- 원인: Safari 구버전 / 오래된 Chrome / 일부 모바일
- 해결: **Chrome 또는 Edge 최신 버전** 으로 접속

### 🐢 "첫 실행이 너무 오래 걸려요"
- 879MB 다운로드는 네트워크에 따라 2~10분까지 걸릴 수 있어요
- 공용 Wi-Fi 보다는 유선 또는 안정적 Wi-Fi 권장
- 진행률 바가 멈춘 것 같으면 **네트워크 탭** (개발자 도구) 에서 실제 다운로드 속도 확인

### 🤔 "답이 어색한 한국어로 나와요"
- **1B 모델의 한계** 입니다. 한국어 학습량이 영어보다 훨씬 적어요.
- Ch02 에서 쓸 **Gemini** 는 한국어 품질이 훨씬 좋습니다.

### 🔁 "두 번째 실행도 오래 걸려요"
- 브라우저 **시크릿/프라이빗 모드** 는 캐시를 안 남겨요. 일반 창에서 실행하세요.
- 데이터 지운 뒤에도 그대로. IndexedDB 를 수동으로 지우지 않았다면 캐시는 보존됩니다.

### 💥 "답이 영어·중국어·코드 범벅으로 나와요"
- temperature 가 1.0 을 넘었거나, maxTokens 가 너무 큰 경우 (1B 한계)
- \`temperature: 0.3 ~ 0.7\` + \`maxTokens: 200\` 이내로 조정`,
    },

    // ─────────────────────────────────────────────
    // 16. 정리 + 다음 장 예고
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `## 🧭 Ch01 정리 + Ch02 예고

### 오늘 배운 것
- ✅ AI 모델이 브라우저 안에서 돌 수 있다 (WebLLM + WebGPU)
- ✅ \`chat({ messages, ... })\` 호출로 AI 와 대화하는 코드 작성
- ✅ 응답 객체에서 \`text\`, \`model\`, \`latencyMs\`, \`tokensUsed\` 읽기
- ✅ \`temperature\` 로 창의성 조절 — 0.0 결정적, 1.0 창의적, >1.0 위험
- ✅ \`role: "system"\` 으로 AI 에게 역할 부여
- ✅ 작은 모델(1B)의 한계 직접 체감

### 다음 장 (Ch02) 예고 — 클라우드 LLM 의 힘
Ch02 에선 **Google Gemini 2.5 Flash** 를 씁니다. 브라우저 밖의 거대한 서버에서 도는 모델이라:
- ✨ 한국어 품질이 훨씬 뛰어남
- ⚡ 응답 속도도 더 빠름 (클라우드 GPU)
- 🆓 **무료 한도 일 1,500 회** — 연습에 충분
- 🔑 **API 키** 를 한 번 발급받아야 함 (Ch02 앞부분에서 자세히 안내)

Ch02 에서 같은 temperature / system 실험을 Gemini 로 다시 해서, **모델 크기가 품질에 미치는 차이** 를 직접 비교합니다.`,
    },

    // ─────────────────────────────────────────────
    // 17. Built with Llama (의무)
    // ─────────────────────────────────────────────
    {
      type: "markdown",
      source: `---

**🦙 Built with Llama** — 이 레슨은 Meta 의 [Llama 3.2](https://www.llama.com/) 모델을 사용합니다.
[Llama Community License](https://www.llama.com/llama3_2/license/) 에 따라 자유로운 교육·상업적 이용이 허용되며, 모델 사용 시 "Built with Llama" 표기가 요구됩니다.`,
    },
  ],

  // ─────────────────────────────────────────────
  // 퀴즈
  // ─────────────────────────────────────────────
  quiz: {
    title: "Ch01 — WebLLM 이해도 확인",
    questions: [
      {
        type: "multiple-choice",
        question: "WebLLM 의 가장 큰 차별점은 무엇인가요?",
        options: [
          "클라우드보다 항상 빠르다",
          "설치·계정·네트워크 없이 브라우저 안에서 AI 가 실행된다",
          "모든 거대 모델을 무료로 돌릴 수 있다",
          "API 키가 자동으로 발급된다",
        ],
        correctIndex: 1,
        explanation:
          "WebLLM 은 '브라우저 내 실행' 이 핵심입니다. 데이터가 밖으로 나가지 않고, 첫 다운로드 후 오프라인에서도 동작하는 대신, 모델 크기·속도는 제약이 있어요.",
      },
      {
        type: "multiple-choice",
        question: "Llama 3.2 \"1B\" 에서 1B 는 무엇을 뜻하나요?",
        options: [
          "Llama 의 1번째 버전",
          "영어로 1billion — 모델의 파라미터 개수 10억 개",
          "1GB 의 저장 용량",
          "1초당 처리 가능한 문장 수",
        ],
        correctIndex: 1,
        explanation:
          "B 는 billion (십억). 1B 는 파라미터 10억 개짜리 모델이라는 뜻이에요. 70B, 175B 등 큰 숫자일수록 똑똑하지만 실행에 많은 자원이 필요합니다.",
      },
      {
        type: "multiple-choice",
        question: "temperature 파라미터의 역할은?",
        options: [
          "모델 크기를 바꾼다",
          "응답 속도를 올린다",
          "답변의 무작위성(창의성) 정도를 조절한다",
          "네트워크 사용량을 줄인다",
        ],
        correctIndex: 2,
        explanation:
          "temperature 가 낮을수록 결정적·일관적, 높을수록 다양·창의적 답변이 나옵니다. 보통 0.0 ~ 1.0 범위를 씁니다. 1.0 을 넘으면 특히 작은 모델은 부서지기 쉬워요.",
      },
      {
        type: "multiple-choice",
        question: "role: \"system\" 메시지는 무엇에 쓰이나요?",
        options: [
          "AI 의 내부 버그를 리포트",
          "API 키를 서버로 전송",
          "AI 의 성격·태도·제약을 미리 설정",
          "다른 언어로 번역",
        ],
        correctIndex: 2,
        explanation:
          "system 메시지는 대화 전체에 걸쳐 AI 의 캐릭터·제약을 정의합니다. 'user' 메시지는 실제 질문, 'system' 은 메타 지시에 해당해요.",
      },
      {
        type: "multiple-choice",
        question: "WebLLM (Llama 3.2 1B) 에서 한국어 답변이 영어·중국어 섞여 부서졌습니다. 가장 가능성 높은 원인은?",
        options: [
          "네트워크 연결이 불안정하다",
          "프롬프트가 한국어여서 그렇다 — 영어로 바꾸면 해결",
          "temperature 가 너무 높아 작은 모델(1B)이 한국어 확률 분포를 유지하지 못하고 붕괴한 상태",
          "WebLLM 자체 버그",
        ],
        correctIndex: 2,
        explanation:
          "작은 모델일수록 temperature 가 높을 때 '부서지는' 임계점이 빨리 찾아옵니다. 같은 프롬프트를 Ch02 Gemini 로 돌리면 temperature 1.0 에서도 한국어가 유지되는 걸 볼 수 있어요.",
      },
      {
        type: "multiple-choice",
        question: "첫 실행이 2~3분 걸리는 주된 이유는?",
        options: [
          "서버가 느려서",
          "WebGPU 초기화 때문",
          "879MB 크기의 모델 파일을 처음 다운로드받아 브라우저에 캐시하는 중이라",
          "AI 가 생각을 오래 해서",
        ],
        correctIndex: 2,
        explanation:
          "WebLLM 은 모델 전체를 브라우저 IndexedDB 에 캐시해서 이후 실행은 네트워크 없이 가능하게 합니다. 첫 다운로드만 오래 걸리고 두 번째부터는 수 초 내 준비됩니다.",
      },
    ],
  } satisfies Quiz,
};
