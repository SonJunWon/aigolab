import type { Course } from "../../../types/course";
import type { Quiz } from "../../../types/quiz";

/**
 * 코스 2 「지식 증류」 · 7강 — Chinchilla & 컴퓨트-최적 (모듈 1)
 * 20:1 법칙, '작은 모델+많은 데이터'. 코드 검증: 로컬 통과(N*=sqrt(C/120)).
 */
export const kd07: Course = {
  id: "kd-07-chinchilla",
  title: "Chinchilla — 작은 모델 + 많은 데이터",
  subtitle: "정해진 예산이면, 큰 모델보다 '적당 모델 + 충분한 데이터'",
  icon: "🐭",
  category: "dl",
  level: "intermediate",
  estimatedMinutes: 22,
  order: 207,
  sections: [
    {
      type: "text",
      content: `# 🐭 Chinchilla — "큰 모델 ≠ 최선"

2022년, 딥마인드가 충격적인 걸 발견해요. 당시엔 "무조건 모델을 키우자!"가 유행이었는데…

> 🃏 **Chinchilla의 발견**
> 계산(돈)이 **정해져 있다면**, 가장 좋은 선택은 **제일 큰 모델**이 아니라 **적당한 크기 모델 + 훨씬 많은 데이터**다.
> 당시 거대 모델들은 사실 **데이터를 너무 적게** 먹어서 '배곯은' 상태였어요.

> 🃏 **20:1 법칙 (경험칙)**
> 컴퓨트-최적 지점은 **파라미터 1개당 토큰 약 20개**.
> 즉 모델이 70억 파라미터면 → 데이터는 약 **1,400억 토큰**이 적당.

> 🪄 **비유**: 식비가 정해진 요리 대회. **제일 비싼 재료(거대 모델) 한 번**보다, **적당한 재료 + 충분한 연습(데이터)** 이 더 맛있는 요리를 만들어요. 재료만 좋고 연습이 부족하면 망하죠.

> 🔑 이게 **"작은 모델이 매력적인 이유"** 의 수학적 근거예요. 잘 훈련된 작은 모델이 덜 훈련된 큰 모델을 이길 수 있다 → 그래서 증류·SFT가 빛납니다.`,
    },
    {
      type: "code",
      title: "🧪 컴퓨트-최적 계산기 — 예산 → 모델 크기 & 데이터",
      content: `계산 예산 C가 주어지면 **최적 모델 크기 N과 데이터 D**를 구해볼게요.
근사식: 계산 \`C ≈ 6·N·D\`, 그리고 20:1이라 \`D = 20·N\` → 풀면 \`N = √(C/120)\`.
\`▶ 실행\` — 예산이 커져도 N과 D를 **함께** 키워야 함을 확인하세요.`,
      code: `# Chinchilla 근사: 컴퓨트 C(FLOPs) → 최적 모델 N, 데이터 D
#  관계:  C ≈ 6·N·D,   D = 20·N (20:1)   ⇒   C = 120·N²   ⇒   N* = √(C/120)
def chinchilla_optimal(C):
    N = (C / 120) ** 0.5
    D = 20 * N
    return N, D

print(f"{'컴퓨트 예산 C':>16}{'최적 모델 N':>16}{'최적 데이터 D':>18}{'D/N':>7}")
print("-" * 60)
for C in [1e18, 1e20, 1e22, 1e24]:
    N, D = chinchilla_optimal(C)
    print(f"{C:>16.0e}{N:>16.2e}{D:>18.2e}{D/N:>7.0f}")

print("-" * 60)
print("핵심 1) 예산이 커져도 N과 D를 '함께' 키워야 함 (D/N 은 항상 20!)")
print("핵심 2) 무작정 큰 모델 ❌  →  '적당한 모델 + 충분한 데이터' ✅")

# 잘못된 선택 시뮬레이션: 같은 예산에 모델만 키우고 데이터를 굶기면?
C = 1e22
N_opt, D_opt = chinchilla_optimal(C)
N_big = N_opt * 4          # 모델을 4배 키우고
D_starved = C / (6 * N_big)  # 같은 예산이라 데이터가 쪼그라듦
print(f"\\n같은 예산에 모델만 4배 키우면 → 데이터가 {D_opt/D_starved:.0f}배 줄어 '배곯은 모델'이 됨")`,
      codeLanguage: "python",
      codeHint: "D/N 비율이 항상 20인 게 20:1 법칙. 모델만 키우면 같은 예산에서 데이터가 줄어 오히려 손해예요.",
    },
    {
      type: "text",
      content: `> 🎯 **(핵심)**
> - 정해진 예산이면 **큰 모델 ❌ → 적당 모델 + 많은 데이터 ✅**.
> - **20:1** — 파라미터 1개당 토큰 ~20개가 컴퓨트-최적.
> - 잘 훈련된 작은 모델이 **덜 훈련된 큰 모델을 이긴다** → 증류·SFT의 토대.

> 💡 **현실 적용**: "우리 서비스에 70B를 쓰는데, 사실 잘 훈련된 13B면 충분하지 않나?", "파인튜닝 데이터는 파라미터 대비 얼마나 모아야 하나(20:1 감각)".

그런데 2026년엔 이야기가 한 겹 더 생겼어요 — **"훈련을 키우기 vs 추론(생각)을 키우기"**. 다음 강에서 마무리합니다. 🧠`,
    },
  ],
  quiz: {
    title: "7강 — Chinchilla 확인",
    questions: [
      {
        type: "multiple-choice",
        question: "Chinchilla의 핵심 발견은?",
        options: [
          "무조건 제일 큰 모델이 최선이다",
          "정해진 예산이면 '적당한 모델 + 훨씬 많은 데이터'가 최선이다",
          "데이터는 적을수록 좋다",
        ],
        correctIndex: 1,
        explanation:
          "계산이 정해져 있으면 가장 큰 모델이 아니라 적당한 크기 + 충분한 데이터가 컴퓨트-최적입니다. 당시 거대 모델들은 데이터를 너무 적게 먹은 '배곯은' 상태였어요.",
      },
      {
        type: "multiple-choice",
        question: "'20:1 법칙'이 뜻하는 것은?",
        options: [
          "파라미터 1개당 토큰 약 20개가 컴퓨트-최적",
          "모델을 20배 키워라",
          "데이터를 20분의 1로 줄여라",
        ],
        correctIndex: 0,
        explanation:
          "컴퓨트-최적 지점은 파라미터당 토큰 약 20개예요. 70억 파라미터면 약 1,400억 토큰이 적당합니다(D ≈ 20·N).",
      },
      {
        type: "multiple-choice",
        question: "같은 예산에서 모델만 크게 키우면?",
        options: [
          "데이터가 줄어 '배곯은 모델'이 되어 오히려 손해",
          "항상 더 좋아진다",
          "데이터가 자동으로 늘어난다",
        ],
        correctIndex: 0,
        explanation:
          "C ≈ 6·N·D라 예산이 고정인데 N을 키우면 D가 줄어요. 데이터가 부족한 큰 모델은 잘 훈련된 작은 모델보다 못할 수 있습니다.",
      },
    ],
  } satisfies Quiz,
  nextCourseId: "kd-08-2026-scaling",
};
