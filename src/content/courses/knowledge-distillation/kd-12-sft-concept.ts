import type { Course } from "../../../types/course";
import type { Quiz } from "../../../types/quiz";

/**
 * 코스 2 「지식 증류」 · 12강 — SFT 개념 (모듈 2)
 * 사용자 의도를 파악하는 파인튜닝, 후처리 지형도(사전학습→SFT→정렬). 코드: 순수 파이썬(지형도 표).
 */
export const kd12: Course = {
  id: "kd-12-sft-concept",
  title: "SFT 개념 — 의도를 파악하는 파인튜닝",
  subtitle: "사전학습 모델을 '말귀 알아듣게' 만들기",
  icon: "✍️",
  category: "dl",
  level: "intermediate",
  estimatedMinutes: 18,
  order: 212,
  sections: [
    {
      type: "text",
      content: `# ✍️ SFT — 모델을 '내 용도'로 길들이기

이제 모델을 **내 목적에 맞게 다듬는 법**, **SFT(Supervised Fine-Tuning, 지도 파인튜닝)** 입니다. 증류로 만든 데이터가 실제로 '새겨지는' 단계예요.

> 🃏 **모델이 만들어지는 3단계 (후처리 지형도)**
> 1. **사전학습(Pretraining)**: 인터넷 텍스트를 잔뜩 읽어 **'다음 단어 맞히기'** 학습. → 똑똑하지만 **말귀를 잘 못 알아들음**(질문해도 또 질문으로 답하기도).
> 2. **SFT(지도 파인튜닝)**: **(지시, 좋은 답)** 쌍으로 "이렇게 답해"를 가르침. → **의도를 파악하고 형식에 맞게** 답하게 됨.
> 3. **정렬(RLHF/DPO 등)**: 사람 선호로 더 다듬기(안전·도움됨).

> 🪄 **비유**: 사전학습 = **책을 엄청 많이 읽은 사람**(지식은 많지만 대화가 서툼). SFT = **'질문엔 이렇게 답하는 거야'를 훈련**해 대화가 되게 만들기. = **기성복을 내 몸에 수선**.

> 🔑 이 코스의 줄기: **증류로 좋은 (지시,답) 데이터를 만들고 → SFT로 작은 모델에 새긴다.** SFT는 증류가 실현되는 무대예요.`,
    },
    {
      type: "code",
      title: "🧪 사전학습 모델 vs SFT 모델 — 같은 질문, 다른 반응",
      content: `왜 SFT가 필요한지 감을 잡아요. 같은 질문에 두 모델이 어떻게 다르게 반응하는지(개념 예시).`,
      code: `질문 = "파이썬으로 리스트 뒤집는 법?"

사전학습_모델 = [
    "...그리고 리스트를 뒤집는 법은 면접에 자주 나온다. 다음 질문:",  # 그냥 이어쓰기(말귀 못 알아들음)
    "리스트 뒤집기는 여러 방법이 있는데",                              # 산만
]
SFT_모델 = "lst[::-1] 또는 lst.reverse() 를 쓰면 됩니다. 예: [1,2,3] -> [3,2,1]"

print("[질문]", 질문, "\\n")
print("❌ 사전학습 모델(말귀 서툼):")
for line in 사전학습_모델:
    print("   ", line)
print("\\n✅ SFT 모델(의도 파악 + 형식):")
print("   ", SFT_모델)
print("\\n👉 둘 다 '지식'은 비슷해요. 차이는 '의도를 알아듣고 깔끔히 답하는 습관'.")
print("   그 습관을 (지시,답) 데이터로 가르치는 게 SFT입니다.")`,
      codeLanguage: "python",
      codeHint: "사전학습 모델은 '다음 단어 맞히기'만 배워서 질문에 엉뚱하게 이어쓰기도 해요. SFT가 '질문→답' 형식과 의도 파악을 가르칩니다.",
    },
    {
      type: "text",
      content: `> 🎯 **(핵심)**
> - **사전학습**(지식) → **SFT**(의도·형식) → **정렬**(선호) 의 3단계.
> - SFT = **(지시, 좋은 답)** 쌍으로 모델을 길들이기.
> - 증류로 만든 데이터가 **SFT를 통해 작은 모델에 새겨진다**.

다음 강에서 파인튜닝이 **무엇을·왜** 바꾸는지 좀 더 보고, 그 다음 모듈 3부터 **실제 파인튜닝 방법(FFT vs PEFT/LoRA)** 으로 들어갑니다. 🛠️`,
    },
  ],
  quiz: {
    title: "12강 — SFT 개념 확인",
    questions: [
      {
        type: "multiple-choice",
        question: "SFT(지도 파인튜닝)가 모델에게 주로 가르치는 것은?",
        options: [
          "새로운 방대한 세상 지식",
          "지시(의도)를 파악하고 형식에 맞게 답하는 습관",
          "모델을 더 크게 만드는 법",
        ],
        correctIndex: 1,
        explanation:
          "SFT는 (지시, 좋은 답) 쌍으로 '이렇게 답해'를 가르쳐 의도 파악·형식을 길들입니다. 방대한 지식은 주로 사전학습에서 와요.",
      },
      {
        type: "multiple-choice",
        question: "모델이 만들어지는 순서로 옳은 것은?",
        options: [
          "사전학습 → SFT → 정렬(RLHF/DPO)",
          "SFT → 사전학습 → 정렬",
          "정렬 → 사전학습 → SFT",
        ],
        correctIndex: 0,
        explanation:
          "인터넷 텍스트로 사전학습(지식) → (지시,답)으로 SFT(의도·형식) → 사람 선호로 정렬(안전·도움됨) 순서입니다.",
      },
      {
        type: "multiple-choice",
        question: "SFT가 이 코스(지식 증류)와 연결되는 지점은?",
        options: [
          "관계없다",
          "증류로 만든 좋은 (지시,답) 데이터가 SFT를 통해 작은 모델에 새겨진다",
          "SFT는 데이터를 삭제한다",
        ],
        correctIndex: 1,
        explanation:
          "현대 증류는 교사가 만든 고품질 데이터로 학생을 SFT하는 것이에요. SFT는 증류가 실제로 작은 모델에 새겨지는 무대입니다.",
      },
    ],
  } satisfies Quiz,
  nextCourseId: "kd-13-finetuning-general",
};
